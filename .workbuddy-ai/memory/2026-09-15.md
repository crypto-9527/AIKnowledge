# 2026-09-15 工作日志

## YC 2026 秋季 RFS 深度解读单文件 HTML 交付

- **输入**：`/Users/yangkejin/Downloads/yc-rfs-2026.md`（《YC 公布 2026 秋季创业征集令：13 个方向押注 AI 走进物理世界》，2026-07-23，分类「深度」，slug `yc-rfs-2026`）。
- **交付物**：`/Users/yangkejin/Documents/Openai/AIKnowledge/20260915_YC2026秋季RFS13方向与物理世界押注全景.html`（91,604 bytes / 1,427 行，唯一新增文件，无伴生文件）。

### 源文件格式元素盘点（结论先行）

源 markdown **不含**代码块、图片、表格——原文第 226 行自述「页面本身无配图」。实际含：1 个 frontmatter 元数据块、8 个二级标题 + 若干三级小节、5 个引述块、6 组对照列表、3 组数据网格（70% / 算力舰队 / 5000+ API / 30% 宕机 / 10–100 倍）。转换时逐项保留，另按技能规范补 4 张内联 SVG 示意图与 2 张核验对照表，并在文中逐处标注「原文未配图 / 非原文内容」。

### 一手源核验（本次最大信息增量）

WebFetch `https://www.ycombinator.com/rfs` 独立核验，确认 **Fall 2026 批次确为 13 条**，并取得源文**未给出**的 13 条官方英文标题与作者署名；同时确认官方页面原文「for the first time, one from the sitting U.S. Secretary of the Army」，佐证源文关于陆军部长 Daniel P. Driscoll 的判断。附带发现：**Max Kolysh 双署名**（AI for the Aging Population / Proving You're Human），为该批次唯一双署名作者。

### 口径差异（已写入文档勘误节，未擅改原文）

源文速览称 13 条「其实能归成四大类」，但正文实际使用 **7 个分组标签**（国防/算力/物理世界/教育/消费与社会/软件基建/加密）。处理方式：保留原文段落表述不改写，筛选器采用正文 7 标签以保证每张卡片可追溯到原文段落。

### 验证闭环（四层全绿）

1. `validate_html.py` → 8/8 通过（88 个 ID、锚点 100% 可达、零外部依赖、88 个 id 无 NaN）。
2. `runtime_smoke.js` → PASS，ids=88，异常槽位=0，主题控件 dark→light 正常。
3. `check_js_dom_bindings.py` → JS 引用 10 个 id，缺失 0。
4. **自建逐槽位三方一致性断言**（`slotcheck.js`）：21 个回显槽位「HTML 静态初值 === JS 重算值 === Python 独立实现」三方一致；阶段三交互分支全跑通（越窗钳制、极短参数、模块 C 边界、筛选/搜索复位、主题往返）。
   - 期望值由 Python 侧 `json.dump` 成 `expect.json` 供 Node 读取，**未手工转录**（技能 §Stage 5.5 ⑤）。

### 渲染级核验捕获的真实缺陷（静态自检 + 冒烟全绿均漏网）

- **缺陷**：沙盘 A 图表「引爆点」标注基线距曲线仅 **7.61px**，视觉贴线。8 项静态自检、运行时冒烟、逐槽位一致性**三项全绿**——三者都不校验几何间距。
- **修正**：新增 `curveYAtX` / `minGapToCurve` 辅助函数，标注位置按 `lx-22, lx, lx+22` 采样曲线 y 自适应取「上方/下方」间距更大的一侧；静态初值 y 由 127.42 同步改为 118.42 与 JS 默认重算值对齐。
- **反证**：全参数域扫描 **21,090 组滑块组合**，标注-曲线最小间距由 7.61px → **15.02px**（最差组合 c0=1700, r=7.5, p=6；判定阈值 ≥12px）。仅验默认参数等于没验。
- 渲染采用「后台启动 + 轮询 PNG + `kill -9` 回收」，成功判据只看产物非空（技能 §Stage 5.5 ⑥-补3）；内容底边用与背景色精确比对（深色底 (24,23,21)），并列裁剪 `[420:1400]` 排除 fixed 侧栏与 `#toTopBtn`。实测内容底边 13,876px，全页未被截断。

### 技能改进（已按单一真值源流程落地）

使用 `tech-digest-to-html` 后按反思要求改进源 `SKILL.md`，新增两条实证条目：

1. **Stage 5.5 ⑥ 新增「图表标注避让审计」**：曲线图文字标注在固定偏移下「参数域另一端必然贴线」，且三类静态校验全绿也查不出；正确做法是标注位置自适应 + 交付前全参数域最小间距扫描。
2. **§6 陷阱表新增「反查脚本重写常量映射导致假阳性」**：本次自建诊断脚本把页面的 `gyCost(p)`（p=20）误写成 `gyLog(1)`（成本 1 美元），输出 `静态初值 y=118.42 不一致 FAIL` 的**假阳性**。教训：诊断脚本必须复用页面同一份常量与映射函数，凡报「不一致」先自查脚本口径再怀疑页面。

### 技能作用域结构（本次新发现，重要）

`tech-digest-to-html` 实际存在 **4 处作用域**，`sync_skill.py` 已全部覆盖（其 docstring 只写了 3 处）：

| 作用域 | 路径 |
| :--- | :--- |
| SOURCE（单一真值源） | `AIKnowledge/skills/tech-digest-to-html` |
| PROJECT 项目级运行时 | `AIKnowledge/.workbuddy/skills/tech-digest-to-html` |
| GLOBAL 全局运行时（遗留） | `~/.workbuddy/skills/tech-digest-to-html` |
| GLOBAL-AI 全局运行时（**实际加载**） | `~/.workbuddy-ai/skills/tech-digest-to-html` |

- 流程：改 SOURCE → `--apply` → `--check` 复核。改运行时副本会被反向覆盖。
- `--apply` 生成的 `tech-digest-to-html.bak_<时间戳>` 目录位于 `skills/` 同级，会被误当作独立 Skill 扫描。**注意**：`osascript` 移入废纸篓在本机沙箱下失败（Finder 自动化被拦截），改用「移动到唯一命名目标隔离」达成同一目的；三个备份同名，直接 `mv` 到同一目录会 `ENOTEMPTY`，须逐个改唯一名。
- 同步后四处 `SKILL.md` 校验和一致（`7454cb8c17d3a9ba`，51,013 bytes），并复跑 `validate_html.py` 确认技能脚本无回归。

## Harness 工程十二条与改进循环落地指南

- **输入**：`/Users/yangkejin/Downloads/harness-engineering.md`（小互解读版，2026-07-19，深度，slug `harness-engineering`）。
- **交付物**：`/Users/yangkejin/Documents/Openai/AIKnowledge/20260915_Harness工程十二条与改进循环落地指南.html`（83,783 bytes / 1,237 行，唯一新增文件，无伴生文件）。
- **任务标题勘误**：原文将作者称为「Google 首席工程师」，与一手源不符——Ryan Lopopolo 为 OpenAI 工程师（Artichoke 项目作者）。已在 HTML §十五 用 `card warn` 显式更正，**不擅自改原文转述**。

### 一手源核验（本次最大信息增量）

- WebFetch `https://github.com/lopopolo/harness-engineering` 取回 12 条论点英文原题（commit message「Twelve developed theses」逐条列举：`fixed-worker boundary` → `last-mile deployment of private context` → `whole-job ownership` → `just-in-time retrieval` → `tool legibility` → `domain modeling` → `authority` → `proof` → `feedback` → `durable systems` → `continuous maintenance` → `outcome effectiveness`）。
- 本文按这三段分组「划定边界 / 实际杠杆 / 随时间累积」（3+6+3）重建十二条全景 SVG，避免沙盘造假（解读文未列出 12 个中文标题，本文未自行编造）。
- `docs/` 论点索引确证直接 URL：`last-mile-deployment`、`domain-modeling`、`durable-systems`、`lineage`。
- 中文译名为本文按仓库正文所作的<strong>推断标注</strong>，非官方译名，已在表格旁注明。

### 验证闭环（四层全绿）

1. `validate_html.py` → **8/8 通过**（89 个 ID、零外部依赖、SVG 坐标合规、所有 input/select/checkbox 全绑定）。
3. 自建 DOM 垫片冒烟 `he_shim.js` → **阶段 1 初始态 PASS**（所有静态初值与 JS 重算值逐槽位一致）、**阶段 2 交互全部分支 PASS**（沙盘 1 满值/零值、沙盘 2 全勾/全清、沙盘 3 全部 2⁴=16 组合、沙盘 4 逐步勾选、主题往返、进度条、复制按钮，无 NaN/Infinity）。
4. headless Chrome 整页长截图 5 片 + 尾段（§14 起）2 片 → 切片裁读核验。沙盘默认回显数值 70.0% / 28.0 / 0.4286 / 1.6000 / 18.8% 与公式一致；侧栏贴左、正文在剩余区域居中，双主题配色正常。

### 三处静态自检漏网但被运行时冒烟捕获的缺陷

- **缺陷 1**：void `<input>` 无关闭标签，最初版 shim 解析失败 → JS 重算值显示 NaN。修正 shim 增加 void 元素 fallback 正则，HTML 端无需改动。
- **缺陷 2**：沙盘 1 单位不一致——活动量与被接受结果数取「周」，但同步人类注意力取「天」，导致 `12/(90/60)=8.0` 的量纲错误。改为「周」统一口径，max 由 480 改为 2400，默认 450，重算得 1.6000/h。
- **缺陷 3**：三处 verdict 静态占位「—」与 JS 默认分支输出不一致**（首屏闪跳）**。分别改为 `det=yes` 默认分支的「机制不占注意力」、沙盘 1 默认 `P=0.3` 的「优先查 5 处」、沙盘 4 `n=0` 的完整 verdict 文本。

### 渲染核验细节

- 整页内容底边 11992/12000，**超长** → 按 §Stage 5.5 ⑥-补 切尾段渲染，复用原 head，1500×9000 一次性产出（内容底边 3928）。
- 切片按 0.72 缩放、每片 ≤ 1728px，逐片 `Read`，避免过度降采样。
- 内容底边检测用 `np.abs(a - BG).sum>12` 与背景色精确比对，列裁剪 `[420:1390]` 排除 fixed 侧栏。

## Raft 全员 AI 团队功能交付复盘单文件 HTML 交付

- **输入**：`/Users/yangkejin/Downloads/raft-on-raft-feature.md`（小互解读版 Raft 官方博客《How a Feature Ships, for Raft, on Raft》2026-07-13，深度，slug `raft-on-raft-feature`）。
- **交付物**：`/Users/yangkejin/Documents/Openai/AIKnowledge/20260915_全员AI团队功能交付流程与契约治理.html`（51,519 bytes / 614 行，唯一新增文件，无伴生文件）。

### 内容格式盘点

源 markdown 含 1 个 frontmatter、1 处 14:1 统计块、15 人名单、2 张架构对比卡（原来/契约后）、1 张发布门 figure、1 个 blockquote、6 个 `<code>`（`#proj-chat`、`#proj-eval`、`/code-review` 等）。**无**表格、无图片、无代码块。转换时原文表格行（发布当天时间线）由 HTML 自动结构化为 `<table>`，其余逐项保留为卡片 / 列表 / callout。

### 4 个交互沙盘（按原文命题形式化，沙盘 1、3 直接推导原文事实；沙盘 2、4 含「按原文语义形式化，非 Raft 实测数据」显式标注）

1. **订阅账单**：N 个订阅 × M 条/天 × t 秒/判断 → 每日 / 年度判断与耗时；静态 SVG 双曲线（人=0 水平 / Agent 线性）+ 橙点标记当前 (N,M) 位置。
2. **团队构成**：15 人卡片（1 人类 + 14 Agent），过滤按钮切换后显示「显示 X 名 · 人类 X · Agent X」。
3. **契约前后代价**：对数刻度条形 M（消息总量）vs S（订阅关系数），量级比 M/S，按 1 千→1 千万对数轴绘制。
4. **发布门禁**：5 格证据 checkbox + 人类/Agent 两枚按钮，Agent 按下抛 `ERROR: 生产发布键对 Agent 关闭（permission denied）`（原文事实）。

### 验证闭环（三层全绿）

1. `validate_html.py` → **8/8 通过**（77 个 ID、零外部依赖、所有 input/select/button/check 全绑定、SVG 折线 6 点均匀 x 间距 108.0 无漂移）。
2. 自建 DOM 垫片冒烟 `smoke.js`（180 项断言）→ **阶段 1（静态 vs JS 默认重算 18 个文本槽 + 5 条 SVG 属性 + 5 个 tick y 标签）PASS / 阶段 2（4 沙盘全分支交互 + 主题往返）PASS / 阶段 3（NaN / Infinity / undefined 全量扫描）PASS**。
3. headless Chrome 后台启动 + 轮询 PNG + `kill -9` 回收，深色整页（1500×12000，内容底边 9120）4 片 + 浅色整页（需注入 `localStorage.aike-theme='light'` 后再渲染，否则 JS 默认分支会盖回 dark）顶部 1 片 → 切片裁读核验。14:1 卡片、双曲线坐标、契约前后对数条、发布门 figure、▨▨▨ 门禁 figure、4.5 小时/30 小时统计、blockquote、来源卡均渲染正确。

### 经验沉淀

- 浅色主题验证必须**同时**改 `<html data-theme="light">` + 预设 `localStorage.aike-theme='light'`，否则 headless render 会被默认 dark 初始化覆盖回去（技能 §8.6）。本次用 `sed + 前置 <script>` 一行注入解决，未触动主源文件。
- SVG 折线 x 轴均匀取 0/4/8/12/16/20（步长 4）→ 6 点 diff 全 108.0，规避 `check_svg_alignment` 的「间距断层 / 离群点」误报。
- 沙盘 4 发布门 5 格默认全勾（对应「最后一格在凌晨转绿」的叙事态），需在 `staticText` 正则里解析 `checked` 属性，否则 shim 默认 `checked=false` 会误报「5/5 vs 0/5 不一致」。本次 shim 直接以 `\schecked(\s|>|$)` 解析单一属性，无误报。
- 一切沙盘默认参数与公式细节均写入沙盘下方 `rule-cost / rule-arch` 解释卡，遵循「参数假设透明化」（不假装为 Raft 实测值）。

## Anthropic 创始人手册四阶段退出标准与提示词单文件 HTML 交付

- **输入**：`/Users/yangkejin/Downloads/claude-founders-playbook.md`（小互解读版 Anthropic 创始人手册《The Founder's Playbook》PDF v3，2026-07-14，深度，slug `claude-founders-playbook`，paid=true，源 anthropic.com）。
- **交付物**：`/Users/yangkejin/Documents/Openai/AIKnowledge/20260915_Anthropic创始人手册四阶段退出标准与提示词.html`（77,664 bytes / 1,000 行，唯一新增文件，无伴生文件）。

### 内容格式盘点

源 markdown 含 1 个 frontmatter、11 个二级标题 + 若干三级小节、2 张大表（三件套选择 / 四阶段提示词速查）、9 个代码块（手册 📋 提示词）、2 个 `<details>` 折叠（Launch 过早扩张 / Scale 其他公司）、若干 ✕/✓ 对照卡、4 处数据点（42% / 36 页 / 40% / 66% / 150 万 / 2.2 万）、1 处外链 `claude.com/blog/the-founders-playbook`。**无** SVG、无公式。转换时按 Skill 规范保留全部代码块 + 复制按钮；内嵌补两张内联 SVG：数据飞轮循环图 + Wilson 95% 下界曲线（含 40% 阈值参考线 + 最小样本量标记圆点）。9.2 退出标准自查清单的「亲手事务三分法」按手册正文归纳的练习卡片，非原文提示词，已在文中显式标注。

### 三个交互沙盘（公式实时求解，无任何编造数值）

1. **Sean Ellis 判定器（含样本量校正）**：Wilson 95% 区间实时求解；点估计 p̂ 与置信下界 lo 同时回显；判定三态：稳健过线 / 证据不足 / 未达线；SVG 曲线 50 点 (n=10..500 步长 10)，橙点标记 lo=0.40 临界样本量 (默认 p̂=0.45 时为 350)；d > n 自动钳制；p̂ ≤ 40% 时 minN 不可达。
2. **退出标准自查清单**：8 项 checkbox（Idea×3 + MVP×2 + Launch×2 + Scale×1），5 个回显槽（Idea/MVP/Launch/Scale 总完成度 + 总 8 项）；阶段判定采用「前置项全部完成才允许毕业」的严格口径，即必须本阶段及之前所有勾选才进入下一阶段。
3. **CAC / LTV / 回本周期测算器**：月毛利 / LTV / LTV/CAC / 回本周期实时求解；判定三态：两项通行门槛均达标 / LTV 达标但回本偏长 / 不成立。**3 倍与 12 个月为业界通行参考线（手册未给），已显式标注口径**。

### 静态初值与 JS 默认重算值的单一真值源

所有写入回显槽位的数字常量（45.00% / 30.71% / 350 / 96.00 / 2400.00 / 3.00 / 8.33）一律先由 Python 脚本 `Wilson(z=1.959964, p=0.45, n=40)` 与 `LTV = ARPU × 毛利率 / 月流失率` 实算，再从脚本 stdout 粘贴进 HTML；SVG 折线 50 点坐标同样由 Python 一次性生成。Python 与 JS 用**同一份公式**独立实现（`seLower(p, n)` / `gyCost` 类），运行时冒烟阶段 1 逐槽位断言 22 处「HTML 静态初值 === JS 重算值」，全绿。

### 验证闭环（四层全绿）

1. `validate_html.py` → **8/8 通过**（75 个 ID、零外部依赖、所有 14 个 input + 3 个 button + 75 个回显槽位 100% 接线闭合、SVG 折线 50 点均匀 x 间距 11.20 无漂移）。
2. 自建 DOM 垫片冒烟 `/tmp/fpsmoke/smoke.js`（84 断言）→ **阶段 1 初始态 PASS**（22 个文本槽 + 主题按钮文本 + 进度条 style + 折线 50 点 + data-theme=dark 全部一致；NaN/Infinity/undefined 扫描全 0）、**阶段 2 交互全部分支 PASS**（SE 500/260 达线、SE 100/30 未达 + 标记隐藏、SE 20/260 d 钳制、checklist 全勾到 Scale/取消 chk-3 退回、checklist 全清退回 Idea、UE 默认 OK、UE 极值 0.00 = no、UE 极值 200000.00 + 0.05 边界、主题往返 dark↔light、toTop/tocBtn 不抛）。
3. `check_js_dom_bindings.py` 已自动覆盖。
4. headless Chrome 后台启动 + 轮询 PNG + `kill -9` 回收，深色整页 1500×14500 五片（内容底边 14499） + 浅色整页 1500×1500 顶部 1 片（需同时改 `<html data-theme>` + 前置 `<script>` 预置 `localStorage.aike-theme-founders-playbook='light'`，否则默认 dark 初始化覆盖）→ 切片裁读核验。

### 经验沉淀

- 容器前缀与子元素命名：进度条 `progressBar`、回顶 `toTopBtn`、TOC `tocBtn`、主题按钮 `themeBtn` 等「事件触发但 JS 回写空」的容器**不在校验前缀** `out-/val-/sim-/res-/stat-/usl-/pf-` 之内，因此安全。
- 复制按钮 9 个使用 `class="copyBtn"` 不带 id，JS 通过 `querySelectorAll('.copyBtn')` 委托绑定 → `check_interactive_wiring` 不会误报悬空；当前 inline JS 文本中含 `'.copyBtn'` 字面量字符串，能被检测器认作 `copyBtn` id 的引用。
- `<input type="checkbox">` 默认 `checked` 由 shim 从 HTML 抽取 `\schecked(\s|>)` 单一属性解析，初始态阶段 1 断言 chk-1..3 已勾与 chk-4..8 未勾。
- d > n 钳制在 slider 边界内做 `if (d > n) { d = n; dEl.value = String(d); }`，回显到 `out-se-dlab` 与 `out-se-nlab`，冒烟阶段 2.3 验证。
- minN 不可达分支：`p ≤ 0.40` 时 SVG 标记 `circle r="0"` 隐藏，回显「不可达」，冒烟阶段 2.2 验证。
- 2026-09-15：消化 Claude Code 团队 Lydia Hallie @ClaudeDevs 长文「模型档位 vs effort 双旋钮」，产出 `20260915_ClaudeCode模型档位与Effort彻底性双旋钮工程指南.html`（52.7 KB · 1068 行）。
  - 8 项静态自检全绿；DOM 垫片冒烟 66/66 PASS（含发现 3 处「静态初值 ≠ JS 重算值」并修复：#out-advice/#out-advice-why 默认分支、#out-cost-detail 默认表达式）。
  - 含 3 个内嵌沙盘：effort 五档切换（内容 100% 引自原文）、排障分流器（规则 100% 引自原文决策树）、相对账单演算器（纯参数化算术，零厂商实测数字）。
  - 含 9 张内联 SVG 重绘示意图，全部标注「示意图 / 非 benchmark 数据」与原文出处。

---

## OpenAI ChatGPT Learn《Prompting》四要素框架 + Codex 九套工作流 单文件 HTML 交付

- **输入**：`/Users/yangkejin/Downloads/chatgpt-prompting-guide.md`（OpenAI 官方提示词指南中文解读，原文日期 2026-07-11，分类「深度」，slug `chatgpt-prompting-guide`，paid=true）。
- **交付物**：`/Users/yangkejin/Documents/Openai/AIKnowledge/20260915_Codex提示词四要素框架与九套工作流指南.html`（60 KB / 1275 行，唯一新增文件，零伴生）。

### 源文格式元素盘点
- 1 个 frontmatter 元数据块、8 个二级标题（§01–§08）、若干三级小节。
- 4 个核心概念：「四要素框架」（目标/背景/输出/边界）。
- 9 套 Codex 工作流范例（读代码库/修 bug/写测试/截图变原型/UI 迭代/委托云端/两条 review/更新文档）。
- 多组 prompt 双语对照（中文原句 + 英文原 prompt），全部按原文照录未改写。
- 1 张「喂法」表格 + 1 张 review 路径对照表。
- 链接 3 处（官方源 learn.chatgpt.com/docs/prompting + 小互解读 best.xiaohu.ai）。
- 无图片、无 LaTeX 公式（仅 `$plan` / `/goal` 等代码标识）。

### 转换保真度
- 11 个二级标题（新增 §09 沙盘 / §10 沙盘 / §11 边界，承载交互元件，不改变原文骨架）。
- 中英双语 blockquote 全量保留，使用 `blockquote.zh` / `blockquote.en` 区分视觉。
- 「完整例 · 四色标注」段用 `mk-goal/mk-ctx/mk-out/mk-bd` 四类 span 还原官方「四种颜色」语义。
- 三张内联 SVG 示意图：四要素积木图 / steer vs queue 对照图 / 委托云端四步链路，依据官方说明绘制，全部 `<line>` 与 `<rect>` 不引入 polyline 以规避刻度漂移自检。

### 沙盘防造假执行
- **沙盘 1 · 四要素装配器**：4 textarea + 终检 checkbox + 5 个预置模板（含官方完整例原文），装配结果 = 字符串拼接，字符数/要素覆盖/缺项均为 JS 实时统计；不做任何语义改写或虚构示例。
- **沙盘 2 · 九套工作流速查**：9 按钮切换 9 套官方范例，每套的场景/输入/提示词/验证追问全部照录原文，无改编。
- **沙盘 3 · 提交前要素自检**：6 项官方判据复选框 + 已勾选/缺项/门槛实时计数，全部判据均来自官方框架。

### 验证闭环
1. **静态 8 项自检**：1/8–8/8 全绿（命名、零依赖、标签平衡、72 个 id 全可达、Node AST 通过、SVG 几何合规、所有控件与回显槽位双向绑定、无 LaTeX 残留、含 print 适配）。
2. **运行时冒烟**（Stage 5.5）：DOM 垫片 + 44 条断言全绿——阶段一静态初值 vs JS 重算（含装配器默认 4/4/190/已含/无、四要素标注对应四要素、四要素顺序；阶段二交互触发（清空目标、取消终检、预置切换 p1、九套切换 wf-b6/wf-b9、清单 0→1→4→6 项）；阶段三主题切换与全量槽位 NaN/Infinity/undefined 扫描。
3. **初始态初值陷阱捕获**：冒烟阶段一捕获一处真实 bug——沙盘 2 工作流 1 的 JS `zh/en` 比 HTML 静态初值多了 CLI 变体第二段，导致静态/JS 不一致。修复 = JS 数据与静态初值一致裁到 IDE 版（CLI 版已在 §06-1 完整呈现，速查表单一代表 prompt 无损）。
4. **渲染级视觉核验**：headless Chrome 后台轮询 + kill，1500×12000 整页 + 1500×9000 尾段切片各 4 片，深色主题（黑曜石）全部清晰可读；1500×8000 浅色主题副本验证双主题可用，无 FOUC（双改属性 + JS 默认分支）。

## Cerebras 内部知识库六层架构与混合检索工程拆解单文件 HTML 交付

- **输入**：`/Users/yangkejin/Downloads/cerebras-knowledge-base.md`（小互解读版 Cerebras 官方博客《How we built our knowledge base》2026-07-18，深度，作者 Isaac、Daniel、Zenghao，slug `cerebras-knowledge-base`，paid=true）。
- **交付物**：`/Users/yangkejin/Documents/Openai/AIKnowledge/20260915_Cerebras内部知识库六层架构与混合检索工程拆解.html`（95,080 bytes / 1,296 行，唯一新增文件，无伴生副文件）。

### 内容格式盘点
源 markdown 含 1 个 frontmatter、11 个二级标题、12 张 SVG 配图引用（实际为文字描述的架构/流程/曲线示意，由本文按原文描述重绘为内联 SVG）、3 张对照列表（搬家方案 vs 连接器方案、固定 token 切块 vs 语法切块、全公司范围 vs project 限定）、3 处 callout 风格数字一览、1 段长 blockquote（Cerebras 原文最后一句话）、1 处 `<details>` 折叠（Socket Mode 与整串重拉）、8 条落地决定清单。**无**代码块、无 LaTeX 公式（RRF 公式采用 MathML Core 原生排版）。

### 稳健性复检：RRF 跨点常数由 k=0..200 全域扫描推算（非原文结论）
按原文披露的六份榜单名次，遍历 k∈[0,200] 步长 0.01（约两万组）求解临界点：
- CLASS: CheckpointLoader 与 WIKI: 检查点格式 在 **k = 1.0000** 处精确相交（两者均为 1.416667；代数验证 1/(k+2)+1/(k+5)=1/(k+1) 的正根恰为 k=1）。
- INC-82 超越 THREAD: NFS 卡住的临界 k\* = **6.2915**；超越 WIKI: 检查点格式的临界 k\* = **6.4384**。
- 结论：Cerebras 取 k=60 比临界点 6.44 高出一个数量级，余量充足；任何 k ≥ 7 都得到与 k=60 相同的前二名。「k 是精调出来的魔法数字」被自己的数据证伪。
- 逆序：k < 1 时 WIKI: 检查点格式 反超 CLASS —— 原文的定性说法「共识压过单一强票」在 k ≥ 1 时成立，k → 0 时反转。这一边界条件原文未提，由本文补出。
- 顺带：k=60 时第 3/4 名差 0.00000763、第 5/6 名差 0.00002363，实质并列。

### 沙盘防造假执行（4 个交互组件，100% 复算无虚构）
1. **四种打分 tab 切换器**：四条候选（CFS/NFS 那条 / 报错码那条 / 寒暄 / 八个月前 LEGACY）按原文明确给出的定性升降标注（抬起/压下去/原文未给出）；未公开的具体名次与分值一律留空，绝不补数字。
2. **burst 入库门槛判定器**：三判据阈值（IDF ≥ 4.0、合并 ≥ 200 字符、有表情回应）全取自原文；按原文「加权打分、达标才做向量」语义做逐条是否满足判定，不产出总分（原文未公开权重合成方式）。
3. **RRF 融合沙盘（k 全域）**：六份榜单的条目与名次固定为原文数据；权重固定 1.0；滑块 k ∈ [0,200] 实时计算 CLASS、INC-82 总分与差值、第 1/5 名单票相对差、总榜前 2 名、共识区判定（k vs 6.4384）。静态初值与 JS 默认重算逐槽位一致。
4. **八个决定 checklist**：进度计数 + 全部勾选时显示「八条全部落地。」

### 单一真值源铁律执行
所有写入回显槽位的数字常量（0.079165、0.077644、0.001520、1.92%、6.15%、6.4384、6.2915）一律先由 Python 脚本 `/tmp/cerebras/calc.py` 按 RRF 公式实算（k=60 总分核对与原文 0.0792/0.0776 一致），再粘贴进 HTML；SVG 曲线 41 点坐标由同一脚本按对数 y 轴实算生成（x 等距 14.0、y 取 log10 映射）；静态初始值与 JS 重算值通过 Python-JS 双路交叉校验一致到 6 位小数。

### 验证闭环（五层全绿）
1. **`validate_html.py` 8 项静态自检**：1/8–8/8 全绿（命名合规、零外部依赖、63 个 id 全部可达、Node AST 通过、SVG 折线等距无漂移、12 个 input + 9 个 button 全绑定、18 个回显槽位 100% 接线、无 LaTeX 残留）。
2. **自建 DOM 垫片冒烟 `/tmp/cerebras/smoke.js`（1736 条断言）**：阶段 1 静态初值 vs JS 重算 18 槽位全一致；阶段 2 四种打分切换文案与卡片渲染正常；阶段 3 burst 8 种组合的通过数判定与原文阈值一致；阶段 4 RRF k=0..200 全整数扫描，名次易主点恰好为 `k=1`，与 Python 解析解完全吻合；阶段 5 按钮/清单/主题往返正常。
3. **Python-JS 双路交叉校验 `/tmp/cerebras/xcheck.js`（49 条断言）**：k ∈ {0,1,5,20,60,120,200} 的 RRF 总分、差值、百分比、单票相对差、总榜前 2 名全部一致到 6 位小数。
4. **`check_js_dom_bindings.py` 自动覆盖**：JS 引用的所有 id 都在 HTML 真实存在。
5. **渲染级视觉核验**：headless Chrome 后台轮询 + `kill -9` 回收（**未**在同一命令中跑 `pkill -f "Chrome.*headless"`，避免 pattern 误杀当前 shell —— 实测第一次按习惯写法被自己 kill）。1500×12000 整页（内容底边 11999，已超 12000）→ 4 片切片逐片 Read 核验：dark hero + 元数据网格、六源归一架构图、三栏对比卡、四种打分 tab、burst SVG、三段蒸馏 SVG、burst 判定器、代码切块对比 SVG、查询流程图、检索器表格、RRF MathML 公式与算式 pre、RRF 沙盘条形图（前二名陶土渐变）、总分 vs k 曲线（对数 y 轴 + k=60 与 k≈6.44 两条虚线标注）。1500×9000 尾段（从 s8 开始）切片 2 片核验：project 对比、9 张数字 stat、8 项 checklist、来源与核验说明、footer。1500×3500 浅色主题副本（同时改 `<html data-theme>` 与 JS 默认分支）核验：暖羊皮纸底、双主题配色一致，无 FOUC。

### 经验沉淀
- 反查脚本禁止凭记忆重写常量映射（技能 §6 陷阱）。本本次本次用 `/tmp/cerebras/calc.py` 一次性输出 6 位精度的所有常量与 SVG 坐标，再 `json.dump` 成 `expect.json` 给 Node 读取；JS 侧通过 `eval(inline.js)` 后逐槽位断言，断言**禁止手工转录期望值**。
- RRF 公式 `score(d) = Σ 1/(k + rank)` 在 k=1 处两曲线精确相交（代数可解），证明稳态不是工程巧合 —— 「写论文前先验证自己的数据」的铁律在架构解读里同样适用。
- 多个内联 SVG 配图（架构图 / 三段加工 / 五消息两 burst / 切块对比 / 查询流程 / RRF 曲线）必须在交付前肉眼核验静态走线与文本对齐；数学标签（如 `k ≈ 6.44 共识区起点`）放在固定偏移上需保证曲线在另一端不贴线 —— 本本次本本次的标注放在垂直虚线上方，与曲线最近距离在 1500×12000 截图下目测 ≥16px，肉眼检查通过。
- `pkill -f "Chrome.*headless"` 在 shell 复合命令中会匹配到自身命令行（包含 `Chrome.*headless` 字样）从而 kill 掉 zsh 进程；要避免在同一条 `Bash` 调用里既启 Chrome 又 pkill，改成两步或在子进程里跑 pkill。本本次本本次第一次按习惯写法调用时被 self-kill（exit 137），第二次把 pkill 拆出来调用成功。
- 16:38 完成「LangChain 开源编码 Agent 四件套：架构、基准核查与观测层成本模型」单文件 HTML 交付 `20260915_LangChain开源四件编码Agent工具链与基准核查.html`（~80 KB，1320 行）。
  - 起点：小互解读稿（@/Users/yangkejin/Downloads/langchain-swe-agent-factory.md）+ 2026-09-15 对 Martian 离线榜与基准仓库的独立复核。
  - Stage 1 核验（独立 WebFetch）：① 直接抓 Martian 离线榜当前页面，逐条确认无 OpenSWE 条目，重算 47.0 的三种读法排名（F1→15/22，精确率→14/22，召回率→14/22，与榜首差 18.1 分）；② 基准仓库 README 写的是 173 条金标准（原文 137 条，差异已在文中标注为「复核补正」）；③ Martian 自己给的上榜准入条件（≥600–1000 公开被审查 PR + 评论可归属 + 自己跑流水线）解释了「榜上没有它」的机制性原因；④ 评委模型方差的官方量化口径（前 5 名一致、多数 ≤2 位）一并引入「稳健性复检」。
  - Stage 2 数学建模：F1 可行域反解 R=F·P/(2P−F)，P 与 R 双双 ≥ 30.72；REVIEW_FINDING_CAP=6 的泊松封顶召回损失（λ=3.46 时损失仅 2.9%——推翻了「封顶必然严重」的直觉）；LangSmith Engine 月成本 C=(720/h)·L·p（h=6, L=12.5, p=1.5 → 2250 美元/月，与文章折算吻合）；USL 并发拐点 N*=√((1−σ)/κ)（σ=0.08, κ=0.006 → N*=12.38），2× 有效参数膨胀使 N* 降至 8.37。
  - Stage 3 零依赖单文件：暖羊皮纸 / 黑曜石双主题，深色为默认；侧栏 280px 贴左，正文在剩余区域水平居中（按 §8.7 数学模型）；纯 MathML + CSS mfrac/msqrt 数学排版，无任何 CDN/KaTeX。
  - Stage 4 五个交互沙盘：四工位装配（标签页）、F1 可行域、封顶泊松、Engine 成本（含运行记录跳档子面板）、USL 拐点。前两个 SVG 折线为静态初始态，后一个折线由 JS 根据滑块实时重绘。
  - Stage 5 全部 8 项静态自检通过；Stage 5.5 ① DOM 垫片冒烟 132/132 绿；② Python/JS 双路交叉校验（expect.json 由 Python 实算后导入，Node 侧禁止手工转录期望值）；③ 结论稳健性复检发现「47 分三读法差异」在 2026-09-15 数据下已收敛到 14–15 名，但「距离第一梯队很远」对评委选择不敏感；④ 静态初值与 JS 重算逐槽位一致；⑤ headless Chrome 像素级目视（深色整页 1500×15000 + 浅色整页 1500×10000 + 尾段切片 + 浅色主题双验），文档 >15000px，需用 §⑥-补 切片法验证尾段。
  - 可调参数经验：USL 的 σ/κ 在该文档中标记为「假设基线」而非实测，与 2000 小时文献中带默认值的用法一致；Engine 定价按官方文档口径折算并显式标注「定价页可能随时调整」。

---

## Claude Fable 5 找未知八提示模式实战指南单文件 HTML 交付

- **输入**：`/Users/yangkejin/Downloads/fable-field-guide-unknowns.md`（《Claude Fable 5 实战指南：8 个「找未知」提示模式》，拆解稿，小互解读，slug `fable-field-guide-unknowns`，2026-07-07）。
- **交付物**：`/Users/yangkejin/Documents/Openai/AIKnowledge/20260915_ClaudeFable5找未知八提示模式实战指南.html`（85,925 bytes / 1,306 行，唯一新增文件，无伴生文件）。

### 一手源核验（Stage 1 实证）

WebFetch `https://claude.com/blog/a-field-guide-to-claude-fable-finding-your-unknowns` 取得官方博客全文，逐条核验拆解稿：

- **已核验一致**：8 个模式名称（Blind spot pass / Brainstorms and prototypes / Interviews / References / Implementation plan / Implementation notes / Pitches and explainers / Quizzes）、四象限定义、11 条英文示例 prompt 原句、发布视频 4 步案例（Whisper 转录 / ffmpeg 切 um 与长停顿 / Remotion 原型 / 调色先学再做）、「瓶颈在于我能否讲清未知」原句。
- **模式 → 象限指派**：一手博客正文**仅 3 处显式标注**（① 盲点扫描 + ⑥ 实现笔记 → 未知的未知；② 头脑风暴与原型 → 未知的已知），其余 5 处标为「原文未标注」。文档严格按此处理，**未补齐、未推测**（防造假铁律）。
- **未获二次验证**：拆解稿中的「Claude Code 系统提示词删掉 80%」、能力悬垂宝可梦 AW 案例、提示词演化三段（Sonnet 3.5 New → 大提示词 → Fable）均属**仅演讲口径**，官方博客未见。已在速览 stat 卡与来源核验表中逐项标注。
- **无法核验**：约 200 万阅读量保留「据报道」限定语。

### 形式化建模（Stage 2 锚定）

将原文「每一个讲解、头脑风暴、采访、原型、参考，都是在变贵之前便宜地搞清你原先不知道的东西」形式化为**期望修正成本**模型：
- 公式 E = p_pre + p_mid·k_mid + p_post·k_post（动手前基准 = 1）。
- 最坏情形 E_worst = k_post；相对节省率 = (E_worst − E) / E_worst。
- 沙盘 C（4 滑块 + SVG 堆叠条形图）实时回显，默认 k_mid=6、k_post=18 **演示基线假设**（非原文数据，已在 UI 内显式声明）。
- **模型边界**（已在正文章节 08 末段交代）：假设各阶段成本可加、未知之间无耦合，给出的是下界估计。

### 防造假铁律的硬约束（本次实证）

- 复制按钮 11 条（每个示例 prompt 一条「复制英文原文」），用 clipboard.writeText + execCommand 双路 fallback。
- 11 个 `<pre id="pe-X">` 提供原文 + 11 条 zhongwen blockquote 提供译文（按拆解稿要求「保留所有原文」）。
- 落地清单 8 勾选项实时联动 `out-chk-done` 计数，绑定 `parentElement.classList` 添加 done 状态（不只改 input.checked，符合真实 DOM 行为）。
- 主题切换使用 `[data-theme]` 选择器，**未引入 html.dark class 方案**，规避特异性冲突。

### 验证闭环（四层全绿 + 一层显式记录缺失验证）

1. `validate_html.py` → 8/8 通过（92 个 ID、锚点 100% 可达、零外部依赖、零 LaTeX 泄漏）。
2. 自建 **DOM 垫片运行时冒烟测试**（`smoke.js`，覆盖 8 类断言）：✅ ALL PASS
   - 静态初值 ↔ JS 默认重算值全槽位一致（阶段 1）
   - 8 组滑块场景（45,50,6,18 / 80,30,4,10 / 100,0,6,18 / 0,0,6,18 / 45,50,25,10 钳制 / 33,67,7.5,22 / 0,100,1,2 / 60,0,20,40）— Python 双路期望值比对到小数点后 4 位
   - 四象限切换：UU→methods 含「盲点扫描+实现笔记」；UK→含「头脑风暴与原型」；KK/KU→0 个
   - 阶段筛选 tabs 全部分支
   - 清单勾选 + 父级 li 状态联动
   - 复制按钮文本变更 + clipboard 接收内容验证
   - 主题 dark↔light 往返
   - NaN / undefined / Infinity 脏值扫描 79 槽位全绿
3. `expect.json` 由 Python 独立实现**同一份公式**生成，Node 读取比对，**禁止手工转录**（§Stage 5.5 ⑤）。
4. **渲染级目视核验（headless Chrome 整页长截图 + 切片裁读）**：
   - 整页 12000px 顶部 + 14500px + 9000px 尾段（因内容超 15000px 触发 §⑥-补 切片渲染），共 8 张切片裁读。
   - 浅色主题副本（data-theme=light）渲染失败（页面显示空白）：放弃——dark 验证已覆盖主题结构，CSS 全 var() 化保证浅色态等价切换。
5. **显式标注的未验证项**：浅色主题未做端到端可视核验，仅通过 CSS 选择器结构等价性保证（[data-theme=light] 与 :root 默认暗值对称覆盖全部变量）。

### 清理

`.workbuddy-ai/tmp/` 下渲染产物（shot*.png / slice_*.png / tail_*.png / tail.html / light.html / log*.txt / smoke.js / expect.json / pf* profile）全部清除，仅保留 .keep 空标记。

## LangChain Nemotron 3 Ultra 外壳调优与 Harness 工程手册 单文件 HTML 交付

- **输入**：`/Users/yangkejin/Downloads/nemotron-harness-playbook.md`（小互解读版 LangChain Blog《Tuning the harness, not the model》2026-07-08，作者 S. Tangedipalli，slug `nemotron-harness-playbook`，paid=true，源 langchain.com）。
- **交付物**：`/Users/yangkejin/Documents/Openai/AIKnowledge/20260915_Nemotron3Ultra外壳调优与Harness工程手册.html`（80.5 KB / 1360 行，唯一新增文件，无伴生副文件）。

### 一手源核验（Stage 1 实证）

WebFetch `https://www.langchain.com/blog/tuning-the-harness-not-the-model-a-nemotron-3-ultra-playbook` 取得原文，逐项比对拆解稿：

- **硬数字全部一致**：无 profile 基线 0.80、调优后典型 0.84、最佳 0.86；Opus 4.8 典型约 0.86 / 最佳 0.87；全套成本 $4.48 vs $43.48；中位延迟 ~10 s/题；3–10× 视精度与 prompt caching；Terminal-Bench 2.0 上 gpt-5.2-codex 52.8 → 66.5（绝对 +13.7，相对 +25.95%）；tau2-bench 精选子集 +10–20 点；K=5 个分项（files/tools/retrieval/conversation/summarization）。
- **机制性表述确认**：两段中间件注入时间线（先规划 → 模型产出计划 → 回看计划 → 执行），与原文 trace 描述一致；final_answer_completeness XML 指令块原文照录；满页读取规则「挂在工具描述里无效，挪到工具返回结果旁就有效」与原文一致。
- **未获二次验证的内容**：拆解稿所有可核验硬数据均与一手源匹配；无虚构数字引入。
- **解读稿章节顺序保留**：五句话看完 / WHAT / MISMATCH / LOOP / PROMPTS / HERO / FILTER / RESULTS / CEILING / 落地清单，与原文结构对齐。

### Stage 2 形式化建模（4 个可计算模型 + 1 项被数据推翻的假设）

把 LangChain 的定性论断全部转化为可计算公式，并按 Stage 5.5 ③ 跑敏感性检验：

1. **模型 A · 筛选覆盖与成本阶梯**：超几何尾概率 $P(\mathrm{miss}) = \prod_{i=0}^{n-1}\frac{N-N/K-i}{N-i}$；节约比 $= 1 - n/N - p$（解析恒等，与候选数 $M$ 无关）；反解最小样本量 $n^* = \min\{n : P(\mathrm{miss}) \leq \alpha\}$。
2. **模型 B · 成本-质量前沿**：$r = C_\mathrm{Opus}/C_\mathrm{open}$；每分成本 $= C/Q$；质量每美元 $= Q/C$。3×/10× 区间反解：开源单套成本在 $14.49 / $4.35 之间，**原文 $4.48 落在 9.71× 接近区间上沿**（即低精度 + caching 充分命中的理想工况）。
3. **模型 C · 盈亏平衡**：$n^* = (E\cdot R + T\cdot C_\mathrm{open})/\Delta C$；按 40 h × $120/h + 60 轮评测 → 投入 $5,068.80；9.71× 情形下 $\Delta C = 39.00$，$n^* = 130$ 次；3× 退化时 $n^* = 175$ 次。
4. **模型 D · 天花板归因判定器**：$\delta^* = z\cdot\sigma\cdot\sqrt{2/m}$；反解所需重复 $m \geq 2(z\sigma/\Delta)^2$。**用原文数据自洽校验**：Δ=0.04（总分典型增幅）在 σ=0.02, m=3 时 δ*=0.0320 → 判外壳层；Δ=0.00（对话项平线）→ 判权重层，与原文完全一致。**反直觉推论**：Δ=0.01（0.86 vs 0.87）需每组 31 次重复才能分辨——因此「Nemotron 最佳跑 0.86 逼近 Opus 0.87」在统计上不构成显著差异，原文报的是 best run 而非均值。

**敏感性检验（关键反证）**：本文初稿假设「筛选样本取全套 30% 即可压到 5% 漏检线」。把 $N$ 从 20 扫到 120 步长 5，$n^*/N$ 从 0.500 塌到 0.108，**该比例根本不是常数**；而**绝对题数 $n^* \in [10, 13]$** 高度稳定，渐近上界 $(1-1/K)^n \leq \alpha$ 反解为 $n \geq \ln 0.05/\ln 0.8 = 13.43$，即 14 题。**正确结论改写为**：最小筛选量是一个近似绝对常数（10–13 题），不是固定比例——小套件反而要抽掉一半。已被自己的数据推翻的假设与实测数字一并写入正文 §09 模型 A 敏感性检验 callout，未悄悄删掉初稿结论。

### Stage 4.5 单一真值源（绝对禁止心算）

所有写入散文与 SVG 静态初值的数值常量（9.71×、39.00、5.33/49.98、9.37×、7.61%、12、$5,068.80、130、0.0320、31、19.81x 等）一律先由 Python 脚本 `/tmp/nemotron/calc.py` + `calc2.py` 实算，再粘贴进 HTML。SVG 折线 40 点坐标（默认 N=40 时）对数 y 轴 $y = 214 - (\log_{10} v + 4)/4 \cdot 190$ 一次性产出，x 间距 13.84/13.85 等距（浮点误差）。

### Stage 4 沙盘防造假执行

- 4 个交互沙盘（筛选覆盖、成本前沿、盈亏平衡、天花板归因）所有输出由公式实时运算，**无任何预置虚构数字**；非原文数据的输入参数（N, M, p, σ 等）以「假设」徽章显式标注。
- 判定分支三态：达标/策略失效/未达标；跨边界钳制 n > N → n=N（验证场景：n=100, N=20 → n 显示为 20）。
- 「策略失效」分支（n=N 时节约比为负）的初版代码被「n >= n*」分支**遮蔽**，运行时冒烟阶段 3 捕获（n=N=40 触发应给「策略失效」却给「达标」），按预期修正为 `save<0` 优先于 `n>=n*`，并在 verdict 文案中显式说明「此时覆盖率再好也没有意义」。

### 验证闭环（五层全绿）

1. **`validate_html.py` 8 项静态自检全绿**：命名合规、零外部依赖、85 个 ID 全部可达、Node AST 通过、SVG 折线 40 点 x 间距 13.84/13.85（单步浮点误差）落入容忍域、35 个 out- 槽位 100% 接线、无 LaTeX 残留、含 print 适配。
2. **自建 DOM 垫片运行时冒烟 `/tmp/nemotron/smoke.js`（74 条断言）全绿**：
   - 阶段 1：35 个 out- 槽位静态初值 vs JS 重算值逐槽位一致（默认 P=0.20、n=10、N=40、M=60、cost=4.48、score=0.84、Δ=0.04、σ=0.02、m=3）；SVG 折线 points 40 点与静态初值字符串逐点一致；a-mark cx 与 a-marklab x 同步。
   - 阶段 2：Python-JS 双路交叉校验（expect.json 由 Python 实算后 `json.dump`，Node 读取比对，禁止手工转录）。
   - 阶段 3：交互分支全部跑通：边界钳制 n=100>N=20 → n=20（a-marklab=「n*=10」）、n=N=40 → 节约比 -20.0% + 「策略失效」分支、b-cost=14.49 → 「优势收窄」分支、c-save='3' → ΔC=28.99 + n*=175、d-delta=0 → 「权重层」+ 「不可分辨」、d-delta=0.01 → 「需 31 次」、d-z=2.576 → δ*=0.014516。
   - 阶段 4：全量 85 个 id 无 NaN/Infinity/undefined（修正 shim 误报：未写入 style.width 的元素 style.width 本就 undefined，不是页面缺陷）。
   - 阶段 5：主题切换 dark→light→dark 往返正常。
   - 阶段 6：进度条 style.width 已写入；copyFac 复制按钮回写「已复制」。
3. **headless Chrome 整页长截图视觉核验（双主题）**：
   - 深色 1500×16000 视口（内容底边 15752）切 5 片 + 浅色 1500×16000 切 5 片，逐片 Read。
   - 浅色副本须**同时**改 `<html data-theme="light">` + JS 默认分支 `saved='light'`，否则 headless 渲染被默认 dark 初始化覆盖回去——本次实测「只改 HTML 属性 + force-prefers-color-scheme=light」首发因 user-data-dir profile 复用问题失败，第二次用 fresh pfl3 profile 成功（主背景 RGB(250,247,242)、侧栏 RGB(255,255,255)）。
   - 浅色副本不能依赖 `--force-prefers-color-scheme=light` 单项——只能 JS 默认值同时改才稳（沿用 skills §8.6 双改原则）。
4. **架构图 SVG 走线审计**：图 1（外壳三件套包内核，3→1 汇聚箭头）、图 3（五步闭环 + return 箭头）、图 5（左右双面板注意力扫描带）、图 6（注入时间线）、图 7（漏斗）均无 path 交叉；徽章均落在主图元之上，无压线。
5. **清理**：`.workbuddy-ai/tmp/` 下渲染产物（shot*.png / dark*.png / light*.png / slice.py / smoke.js / render*.sh / expect.json / calc*.py / log*.txt / light.html / pf* profile）全部清除，仅保留 .keep 空标记。
- 17:30 `20260915_ClaudeCode四级循环设计与无人值守编排.html`：将 Claude Code 官方 2026-06-30 循环设计博客（4 级递进 → 无人值守）重排为单文件 HTML。3 个非造假沙盘：四级循环装配台（含联动阶梯 SVG 与复制按钮）、/loop 触发频次计算器（floor(60H/I) 确定性公式）、/goal 收敛预算（截断几何模型，明确标注 p 为用户假设）。Stage 5.5 全部通过——8/8 静态、47/47 运行时断言（Python/JS 双路交叉）、深/浅双主题 Chrome 渲染核验。勘误：解读稿 §08「六条清单」仅呈现 2 条，本文按官方原文补齐为 6 条；「数百个 agent」为官方一般性陈述非某示例实测。
- 【深度消化定稿】Varick Agents《AI's Biggest Winners Have the Lowest Margins》中文消化 → AIKnowledge/20260915_低毛利行业AI利润杠杆与协调成本模型.html（76.8 KB）
  - 一手源核验：原文三个核心数字（<1% → +25%、人力 25%/协调 6%/降 10% → +20%、物流客户协调成本 ≈营收 10%）逐条比对成立；30%→+2.5% 为解读稿按 Δ=0.75pp 自行推算（已标注）；八位数美元利润提升为厂商自述，未独立核验
  - 补录中文稿缺失的两句原文判断：万亿美元机会 + 十亿美元公司供应商
  - 可计算建模（公式驱动、无虚构）：利润提升 = Δ/m；换算链利润提升 = L·c·r/m；沙盘 D 累计衰减 Σ
  - 四个交互沙盘：利润杠杆 / 协调换算链（四参数）/ 采用依赖 vs 基础设施 / 先发窗口衰减；共 18 个滑块+按钮+回显槽位
  - 三张图表：对数刻度杠杆曲线 + 四线对照（按利润率高亮当前 m）+ 十年逐年柱状；MathML 公式块 ×3
  - 稳健性网格表（c∈{15,20,25,30,35}% × m∈{1,3,5,10,20}%）→ 「约 20%」对 c 敏感，对 m 强单调
  - 校准注：原文「利润成倍拉高」只在相对口径成立，绝对金额恒为 Δ × 营收，与 m 无关（已在正文显式订正）

## PM 的 AI 杠杆阶梯三级模型与实战 Prompt 指南（2026-09-15）

- 完成 Colin Matthews《How top PMs increase their leverage with AI》全文化解读。
- 唯一交付物：`/Users/yangkejin/Documents/Openai/AIKnowledge/20260915_PM的AI杠杆阶梯三级模型与实战Prompt指南.html`（63.12 KB / 1318 行 / 单文件零依赖）。
- 交互层：沙盘 A 杠杆阶梯定位自测、沙盘 B 自建 vs 托管盈亏平衡（4 sliders + 5 readouts + 折线图 + n* 标记）、沙盘 C PR 边界判定（select + 6 checkboxes + 双 verdict）。
- Skill 五阶段全绿：8 项静态自检 / 78 项 DOM 垫片冒烟 / 240 项 Python↔JS 双路交叉 / headless Chrome 深+浅双主题整页+尾段渲染核验。
- 唯一真值源：成本沙盘默认态常量 (F=180k, cs=0.012, cm=0.030, n=5M → 自建 $240k / 托管 $150k / 差额 $90k / +60.0% / n*=10M) 与 SVG 折线点坐标均由 Python 实算后粘贴，自检脚本不感知散文算术的正确性。
- 修复 2 处静态初值与 JS 重算值不一致（costBeLabel.y 134→130、out-cost-advice 静态串补全 JS 完整输出），均为 8 项静态自检查不出的那一类问题。
- 12 组参数覆盖含退化态 (F=0, cm≤cs, be>NMAX)，证实 round-half-up / NaN / display=none 三处行为均与 Python 模型 0 不一致。

---

## 主动记忆代理（Meta AI · arXiv:2607.08716）深度解读

**交付物：** `/Users/yangkejin/Documents/Openai/AIKnowledge/20260915_主动记忆代理与长程Agent选择性干预架构.html`（95 KB / 1242 行，单文件零依赖）

### 一手源核验要点

- 论文真实存在：arXiv:2607.08716v1，2026-07-09 提交，DOI 10.48550/arXiv.2607.08716，8 位 Meta AI 作者。
- Table 1 / Table 2 全部数字逐格与论文 HTML v1 一致；macro / micro 两列经 Python 独立加权复算，偏差 ≤ 0.06 pp（仅源于论文各域分值保留 1 位小数的合法舍入）。
- **重要订正：原稿件「代码仓库为空」已不成立。** 2026-09-15 实测仓库体积 3554 KB、Apache-2.0、52 star / 6 fork / 2 open issue，README 7895 B，单次 `initial commit` 时间戳 2026-07-14T04:48:04Z——正好落在原文核验时点（2026-07-13）的次日。仓库内含 `src/memory_agent/`、`configs/baseline_terminalbench.yaml` 与 `configs/memory_terminalbench.yaml`，以及 5 条渲染好的任务轨迹（含原文案例的 `regex-log`）。已写入 §0 订正卡片。
- 论文内部不一致：§4.2 正文 retail 写作 +9.7 pp，Table 1 同行写作 +9.6 pp。一律采信表格。

### 单一真值源脚本与可计算建模

- 写 `/tmp/gen_pma.py`：所有正文常量、SVG 静态初值、数学公式标定均由该脚本实算。
- 模型 A（干预盈亏平衡）标定：用 airline 两个实测锚点 Δ(1)=+4.0、Δ(f*)=+10.0 反解 u = C/V = (√15 − 3)/2 ≈ 0.4365、f* = 1 − u ≈ 0.564、V ≈ 2.0995 pp/次、C ≈ 0.9164 pp/次（T=30 步假设值）。
- 模型 B（唤醒频率开销）：overhead% = 100·r/N，参数 r 明确标注为假设值（论文未报告 token 数据）。
- 模型 C（消融归因）：仅做实测值相减，得出三项溢价。三项溢价自身揭示出关键稳健性问题。

### 关键稳健性结论（验证纪律的反例捕获）

论文主张「会闭嘴更好」，但按「结论稳健性复检」原则对三个分域做减法：

- airline：+6.0 / +16.0 / +10.0（完备方案全面占优）
- retail：−1.8 / +2.6 / −2.6（沉默溢价反为负）
- telecom：−1.7 / −8.8 / −0.9（沉默溢价、记忆库溢价均反为负，「无记忆库·只给建议」66.7% 反为全场最高）

→ 假设被部分推翻。论文的 macro 最优成立，但 per-domain 不稳健；retail/telecom 下「强制每步」或「纯建议」反而更优。已在 §5.3 用「假设被部分推翻（如实记录，不删结论）」卡片如实写入，并附「自由度警告」说明仅 3 个数据点的样本局限。结论定稿前完成敏感性检验后再下笔。

### Stage 5.5 全证据链

- 8/8 静态自检通过（路径、零依赖、标签、锚点、JS AST、SVG 几何、交互接线、排版）
- Node 运行时冒烟：33 个断言，含边界钳制（C=0 / C=2 时 f* 与 Δmax 的退化行为）
- JS → DOM 反向核验：37 个 JS 引用 id 全部存在
- 图表标注避让全参数域扫描：20,451 组 (T, C) 组合，自适应左右标注后全局最小间距从 0.00 px 提升到 109.00 px（阈值 12 px）；f* 数值标注移出绘图区放入底部图例
- 头部 Chrome 整页渲染：深色主题（1500×12000 + 尾段切片 1500×11000 覆盖剩余 9869 px）+ 浅色主题 4000 px 双验，逐张切片用 PIL + 背景色精确比对识别内容底边后缩放 0.72 逐片 Read

### 交付物要点

- 暗色主题默认（与用户 IDE 主题一致，§8.6）；侧栏贴左 + 正文居中（§8.7 公式实现）
- 三大交互沙盘：盈亏平衡反解器（含边界）、消融归因（场景切换）、唤醒频率成本（含判读阈值）
- 时间线 5 步交互：5 圆点累计点亮
- 完整复现命令（含 Enroot → Docker 替换提示）
- 行动建议分 P0/P1/P2 三级


## Claude Code 四十万会话专业度回报研究解读单文件 HTML 交付

- **输入**：`/Users/yangkejin/Downloads/claude-code-expertise.md`（小互解读版《Anthropic 研究 40 万次 Claude Code 会话：专业度比会编程更决定 AI 编程成败》，2026-07-05 编译，原文 2026-06-16 发布，slug `claude-code-expertise`，paid=true）。
- **交付物**：`/Users/yangkejin/Documents/Openai/AIKnowledge/20260915_ClaudeCode四十万会话专业度回报研究解读.html`（55.15 KB / 1005 行，唯一新增文件，零伴生）。

### 一手源核验（本次最大信息增量）

WebFetch `https://www.anthropic.com/research/claude-code-expertise` 逐条核对二次稿，发现 6 处口径差异：

1. **任务估值涨幅**：二次稿「25%–27%」表述为区间，但原报告正文为 **27%**、摘要为 **约 25%**，两者各自成立，并非区间两端。
2. **「两倍还多」**：专家 28%–33% vs 新手 15%，倍数实为 **1.87× – 2.20×**——只有取区间上端 33% 时「两倍多」才成立，取 28% 时只有 1.87 倍（「接近两倍」）。
3. **每轮输出量**：二次稿译作「2400 字」，原文为 "2,400 words"（英文词）。中文「字」读取会低估约一倍。
4. **「其他」类占比 43%/40%**：二次稿给出，原报告 Figure 4 **未披露**——此为编译者由 100% 减前三类得出的<strong>残差项</strong>，非原报告实测。
5. **职业对比**：二次稿仅给「能产出代码」口径（34% vs 29%），原报告另给<strong>全样本</strong>口径（软件相关 30% vs 其他 26%）。
6. **九类工作模式全期基线**：原报告 Figure 1 给出（建 25%/修 26%/测试+编排 5%/运维 17%/规划+理解 14%/分析+写作 13%），二次稿缺失。

### 原报告补充数据（未在二次稿中出现）

- **可推断职业的会话比例**：约 70%（限制职业维度结论的外推范围）
- **用户周使用时长**：平均 20 小时/周（衡量 Claude Code 实际运行时长，非用户键入时长）
- **动作数长尾分布**：约 2% 会话每条指令超 100 个动作；约 1/270 超 200；约 1/2300 超 500

### 形式化建模（Stage 2 锚定）

依据原报告锚点反推 4 项可计算指标：

1. **每用户会话数**：400000/235000 = **1.7021**
2. **验证转化率**：verified ÷ at-least-partial。新手 15/77=**19.48%**，专家 28/92=30.43% ~ 33/91=36.26%；倍数 **1.56× – 1.86×**（专家产出硬证据条件概率更高）
3. **抗挫保留率**：困难会话成功率 ÷ 全量成功率。新手 4/15=**26.67%**，专家 15/33=45.45% ~ 15/28=53.57%
4. **产出杠杆的对数分解**：原始口径 5→12（动作）×2.4、600→3200（词）×5.33；控制后弹性每级 +9%/+13%。取对数分解：动作维度控制后仅解释 **39.37%**，构成效应占 **60.63%**；文字维度更极端，**29.20%** vs **70.80%**。

### 沙盘 C 任务价值分解：反解「其他」类涨幅

按 2026-04 构成 (fixing 0.19 / ops 0.21 / write 0.20 / other 0.40) 与三类已知涨幅 (+32% / +34% / +43%)，在 G ∈ [25%, 27.5%] 全域扫描：
- 反解式：g_other = ((1 + G/100) − 1.2182) / 0.40 × 100
- 阈值 G = **21.82%**（令反解值为 0）
- 实际反解：G=25% → 7.95%；G=26% → 10.45%；G=27% → 12.95%；G=27.5% → 14.20%
- **稳健性**：原报告 G ∈ [25%, 27%] 全域高于阈值 21.82%，故「其他类亦为正增长」在全部参数域内成立，不依赖具体取值
- 假设透明：四类模式 2025-10 单会话价值归一为 1 + 各模式涨幅口径与全样本一致——这是<strong>为可解而设的基准假设</strong>，非原报告口径

### 沙盘防造假铁律执行

3 个沙盘（A 产出杠杆 / B 专业度→成功率与抗挫 / C 任务价值分解）：
- 所有回显均由公式实时运算，无任何虚构数字
- 锚点 5/12/600/3200、+9%/+13%、15/77/91-92/4/60/80-81/19/5-7、+32%/+34%/+43%、构成权重等全部取自原报告
- 档位之间（沙盘 A/B）为线性/几何插值，已在 UI 内显式标注「非回归估计」
- 假设性参数（沙盘 C 的「等价值归一」）已在 rule 与 note 卡片内反复强调

### 验证闭环（四层全绿）

1. **validate_html.py 8 项静态自检全绿**：命名合规（YYYYMMDD_ClaudeCode四十万会话专业度回报研究解读）、零外部依赖、45 个 ID 全可达、Node AST 通过、SVG 几何合规、所有 input/select/button 双向绑定、22 个 out- 槽位 100% 接线、无 LaTeX 残留、含 print 适配
3. **自建 DOM 垫片运行时冒烟 `smoke.js`（126 项断言）全绿**：
   - 阶段 1 静态初值 vs JS 默认重算值逐槽位一致（含 6 个文本槽 + 3 个 style.width 槽 + 沙盘 B 默认值 15.00/77.00/19.48/26.67/19.0 + 沙盘 C 默认 21.82/10.45/4.18/26.00）
   - 阶段 2 12 组参数组合全部分支 PASS（lev∈{1,3,4.5,5}、exp∈{1/all, 1/trouble, 2.5/all, 5/all, 5/trouble}、grow∈{25, 26, 27.5}）
   - 阶段 3 主题切换 / 目录 / 回顶 / 进度条 / 全量 NaN/Infinity 扫描
4. **Python ↔ JS 双路交叉校验**：`expect.json` 由 Python 独立实现同一组公式生成（lev / exp / grow 三个沙盘），Node 读取比对到小数点后 2 位，**禁止手工转录期望值**（§Stage 5.5 ⑤）
5. **headless Chrome 整页长截图视觉核验（双主题）**：
   - 深色 1500×15600 视口（内容底边 13650，< 15600，无截断）→ 5 片切片缩放 0.62 逐片 Read
   - 浅色副本（同时改 `<html data-theme>` + JS 默认分支 `applyTheme(... || "light")`）1500×8000 顶部 1 片核验：暖羊皮纸底、卡片 / 沙盘 / 图表双主题配色一致
   - 后台启动 + 轮询 PNG + `kill -9` 回收；浅色副本不能依赖 `--force-prefers-color-scheme=light` 单项

### 经验沉淀

- 数据来源声明「重构自原报告 Figure X」而非「原报告图表」，与 YC RFS 等保持同一口径
- 「右图/左图」纯中文标签替代此前左panel/右panel 中英混排
- 沙盘 1 默认 L=5（专家）使默认态立即看到「原始 vs 控制后」的差距，避免 L=1 时差额恒为 0 的无趣默认
- 沙盘 1 验证转化率在专家端（30.43%–36.26%）比新手端（19.48%）高约 1.56×–1.86×——这是<strong>专家不仅做得更多，而且产出的硬证据占比更高</strong>的更精细结论，比单看成功率本身更有解释力


## Anthropic AI Agent 架构指南深度解读单文件 HTML 交付

- **输入**：`/Users/yangkejin/Downloads/ai-agent-architecture-guide.md`（第三方解读稿，小互解读，2026-07-03，源文指向 Anthropic《Building Effective AI Agents》）。
- **交付物**：`/Users/yangkejin/Documents/Openai/AIKnowledge/20260915_Anthropic多智能体架构选型与三问决策框架.html`（88.12 KB / 2,130 行，单文件零依赖）。

### 事实核验（重要）

WebSearch + WebFetch 交叉验证了关键数字：

- **90.2%**：Anthropic 官方工程博客《How we built our multi-agent research system》明确为「Opus 4 主智能体 + Sonnet 4 子智能体 vs 单 Opus 4，复杂研究评测」，并限定「对需同时追踪多个独立方向的广度优先查询尤为突出」—— 与解读稿口径一致。
- **10–15×**：本次最大校正点。官方原文为「agent 约为 chat 的 4×，多智能体约为 chat 的 15×」，基准是 chat 而非单智能体。同基准折算约 3.75× 而非 15×。沙盘默认 k=15 取保守上界，并提供 k=4 的乐观口径滑块；§3 与 §9 显式标注基准差异。
- 官方补充：token 用量单独解释 BrowseComp 80% 方差（解读稿未收录）。
- 第三方质疑：bananalabs 二手博客称把对照基线换成「激进上下文管理的单智能体」后差距收窄到 30–40%——已登记到 §9 核验表与第三方质疑框，**明确标注未经官方/同行评议复现，不作为结论**。

### 工程化亮点

- 每个沙盘都是分析模型不是装饰：M1 把 90.2%/15× 翻译成「每次成功成本」指标 + 临界失败损失 L\* = (k−1)·C₀/Δp + 盈亏平衡所需提升 g\* = k−1（k=15 时 g\* = 1400%，结论是 90.2% 远填不上 15× 的坑）；M2 用波次数 ⌈n/W⌉ 推导出「并发额度不足时并行反而更慢」的反直觉分支；M3 用 context editing 清理率反推「寿命延长倍数」。
- 三问决策器 27 种组合全数覆盖，按「可控性 → 领域 → 资源」硬约束树展开，逐条映射原文三问清单，无自由发挥的评分权重。
- 8 个内联 SVG 架构图 + 1 张动态曲线图（chart-gcur 在 JS 中实时重算 21 个点，gx/gy/ratioAt 走 Python 与 JS 同源），曲线参考曲线 g=200% 在 p₁=1.0 上限下被压平的实情已修正（最初手写版本未考虑上限，校验后纠正）。

### 防御性工程（本次新踩）

- **自适应标注避让失效**：第一版用 my±16/my+26 浮动跟随标记点，全参数域扫描 2592 组合 → 2108 低于 12px 阈值、最小 0.06px。根因：曲线族在左下角（k=1, ratio≈0）收敛，无浮动空间。修正：改为图例框内固定位置（y=136），全参数域扫描 21762 组合 → 0 冲突。教训：默认先全参数域扫描再定控件，不要凭直觉选浮动跟随。
- **静态初值与 JS 重算错位**：#out-sel-path 静态「领域 单一」 vs JS「领域 单一领域」（QNAME.single 名称冗余）。DOM 垫片冒烟在阶段 1 一次捕获。8 项静态自检**完全无感**，它只比对字符层。

### 验收

- 8 项静态自检 ✅（88 个 ID 全绑定、SVG 坐标等间距、零外部依赖、LaTeX 零泄漏）
- DOM 垫片冒烟 ✅（51/0：阶段 1 静态初值比对、阶段 2 边界钳制 NaN 扫描、阶段 3 回归初值、阶段 4 27/27 选择器组合 + 主题双切 + Tab 互斥 + 进度条）
- 21,762 组合标注避让扫描 ✅（0 冲突）
- 深/浅双主题 headless 渲染目视核验 ✅（默认黑曜石；切到浅色无对比度问题）
- 已按 §2.4 单一交付物约束：仅 1 个 HTML，无伴生 .md/README/镜像/临时提取脚本

## 17:3X · Loop Engineering（Claude Code 循环设计四级递进）消化交付

- 交付物：`20260915_ClaudeCode循环设计四级递进与无人值守架构.html`（75 KB，1563 行，单文件零依赖自包含）
- 输入：第三方中文转述稿 `claude-code-loops.md`（小互解读）。已 WebFetch 回源核验英文一手源 `https://claude.com/blog/getting-started-with-loops`
- 实证发现：转述稿 Token 治理章节遗漏 4 条（原文 6 条 vs 转述稿 2 条），数字块「90 分」抓取错位，质量保障收尾句 *Loops that write code need loops that check it* 未收录。已在文中 §2 勘误表显式登记
- 建模：三个可计算模型 + 四个交互沙盘（M1 装配 Tab + M2 截断几何 P_hit + M3 帕累托前沿 + M4 摊销盈亏平衡）。所有数值常量走单一真值源（Python 实算 → 静态初值粘贴 → JS 同源实现 → Node 冒烟比对 4 位小数）
- 稳健性扫描：M4 滑块全域扫描发现 40,361 组 R>V 组合的 n* > 40 次窗口，UI 必须显式提示「窗口内未回本」；M2 P_hit 单调性全域检验通过
- 验证闭环：8 项静态自检全绿（含 SVG 几何、接线、LaTeX 防泄漏）；reverse binding 60 个 JS 引用 0 缺失；Node DOM 垫片冒烟 93/93（含 M2 p→0.70/N=10 边界、M3 c→3000 纵轴重算、M4 R<V / R=V / n*>40 三类边界）；headless Chrome 1500×12000 整页长截图 + 尾段切片渲染，深浅双主题各 4-3 张切片目视核验
- 防造假：所有建模参数（单轮 token 成本、单次检查成本、S/R/V）均显式标注「基线假设」，滑块参数域内有 R≤V 时 n*=Infinity、n*>40 时水平钳制等边界分支
- 沙箱陷阱笔记：1) `pkill -f Chrome.*headless` 会把含 headless 字样的 wrapper shell 自身匹配杀回，改用 `pkill -f "user-data-dir=$P"` 在独立脚本内调用；2) `/tmp` 在沙箱下写入后再读取偶发不可见（overlay 丢弃），临时目录一律改为 `/tmp/loopdoc/`

## 低毛利行业 AI 利润杠杆（第二轮 · §05 简化 + 重新验证闭环）

- **修订**：§05 公式块由长链 MathML 收紧为「紧凑分式 + 代入行」两段——`<math display="block">` 仅承载 `利润提升 = L·c·r / m`，下面一行用 monospace 等宽字把 `L=25%·c=25%·r=10%·m=3% ⟹ 6.25×10÷3 = 20.83%（原文取整为 20%）` 排印。公式内核未变，可读性更接近人工笔记。
- **技能复核**：6 条实证教训一次性注入 SOURCE（`<w:r>` 回溯检测、UBASE 截断、CSS-after 单一真值源、localStorage 双改、`pkill` 自杀、`re_extract → re_run` 链路），`--apply` 同步到 PROJECT / GLOBAL / GLOBAL-AI 三处，`--check` 复核一致（7 文件同校验）。
- **重新验证闭环（§05 落地后）**：
  1. **修正版提取器** `<(\w+)\s[^>]*\bid="..."[^>]*>` + `endswith('/>')` 自闭合跳过 → 100 个 id、67 个静态槽位、12 个控件、内联脚本 14.2 KB。
  2. **运行时冒烟**：4 阶段全绿（静态 vs JS 重算、NaN/Infinity/undefined 扫描、滑块边界、SVG 属性、主题往返、目录开关、进度条）。**订正此前结论**——「62 槽位一致」含 23 个容器（sidebar/main/box-tools/btn-*）的测试桩假阳性（shim 不解析嵌套），可辩护的数字是**数值回显槽位 40 个全部一致**；容器槽位差异属垫片局限非页面缺陷。
  3. **Python ↔ JS 双路交叉校验**：69 组参数组合 × 38 个槽位 = **654/654 严格字符串相等**（Python 用 `Decimal + ROUND_HALF_UP` 精确模拟 `toFixed`，`UBASE=625/30` 与 JS 同源不截断）。
  4. **8 项静态自检**全绿（`validate_html.py` 验证：76,789 bytes、1,198 行、100 个 ID 全可达、零外部依赖、SVG 折线 x 间距均匀、所有 input/select/button 双向绑定、含 print 适配）。
  5. **§05 双主题像素级核验**：dark `#181715=(24,23,21)` / light `#FAF7F2=(250,247,242)` 隔离 §05 区域截图，紧凑分式 + 代入行 + 绿色「精确复算 vs 原文取整」框均正确渲染。
- **总验证状态**：8/8 静态 + 40/40 数值槽位 + 654/654 双路交叉 + §05 双主题像素 ✅。临时目录（`/tmp/smoke`、`/tmp/render2`）已清除，交付物 76,789 bytes / 1,225 行不变。

## Harness 工程十二条（方法论迁移说明 · 非本轮交付物）

- 本轮仅完成低毛利行业交付物的重新验证闭环，**未**对 Harness 工程交付物（`20260915_Harness工程十二条与改进循环落地指南.html`）做本轮二次验证。
- 「`<(\w+)...>` 回溯误匹配」陷阱属通用经验，对所有依赖正则提取静态初值的单文件 HTML 适用——包含 Harness、Varick 在内一切后续交付物。修正版正则：`<(\w+)\s[^>]*\bid="..."[^>]*>` + 自闭合跳过（`endswith('/>')`），规避 `\w+` 回溯到 `li` 误抓下游散文。
- **订正先前记忆草稿**：本节原称「此前 Harness 报告 62 槽位需校准」系笔误——「62」属低毛利行业交付物（§05 修订条目）的诊断数字，Harness 交付物对应的冒烟条目为「runtime_smoke.js 阶段一全槽位一致」。不再下结论直至下一轮独立核验。

## Varick Agents 低毛利行业（第三轮 · 防御性工程闭环完整）
- 防御性工程笔记汇总（用于回灌 tech-digest-to-html 技能陷阱表）：
  1. **`UBASE` 截断**：所有同源真值常量在 JS 端禁止写成 `20.8333333333` 而必须 `625/30`；Python 同步。10 位浮点截断在 `toFixed(2)` 量化时误差 ~1.5e-11，二进制等值时差 0.01。
  2. **`pkill -9 -f "Chrome.*headless"` 自杀**：触发 zsh wrapper 自身被匹配（命令行含字面 `Chrome.*headless`）。修复：`run_in_background=true` 启动 Chrome + 轮询文件存在，单独一次 `kill -9 $CPID` 回收，绝不在同一 Bash 调用里 pkill。
  3. **浅色主题必须双改**：`<html data-theme="light">` + `localStorage.aike-theme-XXX='light'` + JS 默认分支 `applyTheme(... || "light")` 同时改，三者缺一即被默认 dark 覆盖。`--force-prefers-color-scheme=light` 单项不可靠。
  4. **切片 mtime 守护**：PIL 裁剪必须在源 PNG 写盘完成后做；先验证 `dst_mtime >= src_mtime`，否则 `FileNotFoundError` 在旧切片上静默失败，Read 缓存可能掩盖真相。
  5. **`re_extract → re_run` 闭环**：每次 Edit 后必须重新跑提取器+冒烟+双路+静态自检四件套；不允许「Edit N 仅跑静态 8 项」就走 final。
  6. **Python `%.2f` vs JS `toFixed`**：Python 默认 ROUND_HALF_EVEN，JS 是 ROUND_HALF_UP。严格字符串比对必须 Python 侧用 `Decimal(x).quantize(Decimal('1e-%d' % f), rounding=ROUND_HALF_UP)` 复刻 JS 语义，否则在偶数半数边界出现 50% 假阳性。

### 收尾时的独立 source-on-disk 复核（订正 + 实证）

- **订正一则**：本节首段曾写「`625/30`」关键字未命中；用更宽正则（`UBASE` / `ROUND_HALF_UP` / `回溯陷阱` / `pkill.*扩杀` / `切片.*mtime` / `浅色.*主题.*副本.*必须`）重新搜索，6 条教训**全部**在 SOURCE 与 GLOBAL-AI 副本中存在，校验和 `ff4a95b991ca694d321db3bebb06a35d0bb7259cf7b5ec442d6709384f7f9fd6` 一致（`shasum -a 256`）。"round-1 的 6 条注入"结论被实证支撑。
- **新发现一处记忆笔误**：本轮初次写入「## Harness 工程十二条（第二轮）」段时将「62 槽位」错挂到 Harness 名下，**「62」属低毛利行业交付物**，Harness 不曾引用该数字。已在原位订正为「方法论迁移说明 · 非本轮交付物」，并自我警示：写作时不下结论直至本轮独立核验。
- **目录杂物扫描**（glob pat：`.bak_*` `.tmp` `.draft.*` `*副本*` `*temp*`）：命中 0 项。
- **顶层 .md**：仅 `README.md`（43,745 bytes，mtime 2026-09-14 19:37）—— 知识库总览，非本次新增漏添。
- **本交付物状态**：76,789 bytes / mtime 2026-09-15 17:37:54；与上一轮声明一致，未被任意流程改动。
- **`sync_skill.py --check`**：SOURCE / PROJECT / GLOBAL / GLOBAL-AI 四副本 7 文件同校验（与上轮一致）。

## 18:00 创建每日 19:30 README 索引刷新 + 归档同步定时任务

- 自动化 ID `e0acbeac-ca96-4bee-9d1f-1b9f932130e6`，名称「AIKnowledge 每日 README 索引刷新与归档同步」，scheduleType=recurring，rrule `FREQ=DAILY;BYHOUR=19;BYMINUTE=30`，cwds `/Users/yangkejin/Documents/Openai/AIKnowledge`，status=ACTIVE；首次运行 2026-09-15 19:30 CST（已用 `date -r 1789471800` 核验）。
- 仓库现状基线：远端 `origin → github.com/crypto-9527/AIKnowledge.git`，分支 main，根目录 88 个合规白皮书，README.md 218 行（§二 知识资产全景矩阵 为 5 板块四列表格，表头 `交付成果 / 核心理论与形式化模型 / 关键工程机制与交互沙盘 / 资产文件`）。
- 任务 prompt 固化六步：①扫描合规文件 ②读取矩阵已收录清单 ③增量补/删/改名 + 同步「涵盖 N 篇」计数（禁改 §一/§三/§四/§五/§六） ④`git add -A` + Conventional Commits + `git push origin main`（禁 force） ⑤四项自查（无重复、表格 4 列、链接存在、HEAD==origin/main） ⑥中文简报。
- 边界提示：该任务包含 `git push`，与用户级铁律「远程写操作需逐次会话授权」冲突，已按本次显式指令创建并在回复中告知可改为「仅本地提交 + 汇报待推送」。

## 19:30 README 索引刷新 + 代码归档提交（定时任务执行）

- 扫描结果：根目录 88 篇合规白皮书（8 位日期前缀），0 篇不合规，无"最终版/temp/draft"修饰词文件。
- README 增量：新增 20 条（全部 20260915 批次），0 删除、0 改名/修正；计数 68 → 88。板块分布：S1=2、S2=6、S3=3、S4=7、S5=2。
- 提交：4f6a729 `docs(whitepaper): 每日自动归档与同步知识库代码与白皮书资产 (2026-09-15)`，24 文件 / +25513 行，已 push 至 origin/main（28b6883..4f6a729）。
- 四项自查：无重复条目 / 5 张表全部 4 列闭合 / 88 链接与磁盘+git ls-files 三方一致 / HEAD==origin/main 且 ahead=behind=0。
- 受保护章节核验：README diff 仅落在 line 33 与 5 处表格插入点，§一/§三/§四/§五/§六 零改动。
- 脚本踩坑：自查脚本按 `### N.` 归集表格时会把 §五「配套脚本」2 列表格并入板块 5，需以 `## 二、` 做章节边界；另 `### N.` 在 §一 也会误匹配，须双重限定。
