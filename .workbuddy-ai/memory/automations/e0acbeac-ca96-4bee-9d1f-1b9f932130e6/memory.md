# 自动化执行记忆 · README 索引刷新 + 代码归档提交

## 2026-09-15 19:30（首次执行）

- 结论：全流程完成并推送。扫描 88 篇（全部合规，0 不合规）；README 新增 20 条（20260915 批次），无删除/改名；计数 68 → 88。
- 提交 `4f6a729`，24 文件 / +25513 行，已 push `28b6883..4f6a729`（main → origin/main，无 force）。
- 四项自查全通过：无重复 / 5 表 4 列闭合 / 索引=磁盘=git ls-files=88 / HEAD==origin/main 且 ahead=behind=0。
- 受保护章节（§一/§三/§四/§五/§六）零改动，已用 `git diff HEAD~1 HEAD -U0 -- README.md | grep @@` 实证。

### 下次执行注意事项

1. **自查脚本边界**：归集矩阵表格时，必须以 `## 二、` 为章节起点、并在遇到任意 `## ` 行时重置 section，否则 §一 的 `### 1./2./3.` 与 §五「配套脚本」的 2 列表格会被误并入，产生假 FAIL（本次已踩）。
2. **新文件均为最新日期**：若新增批次日期晚于表内既有条目，直接追加到对应板块表格末行即可满足「按日期顺序插入」。
3. **commit type 判定**：本次含 20 篇新白皮书 → 用 `docs(whitepaper)`；若某次仅 README 变更则用 `docs`；若含可运行脚本变更再考虑 `chore`/`feat`。
4. **授权提示**：任务含 `git push`，与用户级铁律「远程写操作需逐次会话授权」存在张力。本次按自动化 prompt 的显式指令执行，已在汇报中说明；如用户希望改为仅本地提交，应调整任务 prompt 第④步。
5. **附带的非白皮书变更**：本次顺带提交了 `.workbuddy-ai/memory/2026-09-15.md`、`.workbuddy-ai/tmp/.keep`、`skills/tech-digest-to-html/SKILL.md`（+2 行实证条目），均因 `git add -A` 纳入。若不希望 memory/tmp 入仓，需补 `.gitignore`。

## 2026-09-16 19:30（第 2 次执行）

- 结论：全流程完成并推送。扫描 95 篇（全部合规，0 不合规）；README 新增 7 条（20260916 批次），无删除/改名；计数 88 → 95。
- 提交 `07d271c`，14 文件 / +13919 行，已 push `4f6a729..07d271c`（main → origin/main，无 force）。
- 四项自查全通过：无重复 / 5 表 4 列闭合 / 索引=磁盘=git ls-files=95 / HEAD==origin/main 且 ahead=behind=0。
- 受保护章节（§一/§三/§四/§五/§六）零改动，diff hunks 仅落在 §二 内（L33 / L46 / L77 / L99+100 / L135+136 / L153）。

### 本次新增的经验

1. **7 篇新增的板块归位基准**（下次可直接复用判定逻辑）：
   - §1 SDLC：OpenAI 九步软件工厂（九步闭环 + PR 吞吐排队论）。
   - §2 Harness/Agent 架构：Devin 原生 macOS（云端 Agent 工作区五层验证栈）→ 判为「执行环境/Harness」而非 QA。
   - §3 垂直场景：Anthropic 中小企业 43 工作流、Salesforce AIforce（CRM 解耦）。
   - §4 团队实战：PM 叙事开发循环、FDE 两道闸门（FDE 类一律归 §4，与既有 L106 FDE 面试指南对齐）。
   - §5 商业战略：64 条增长策略（与既有「80 种赢法」同格）。
2. **提交时工作区杂项**：本次除新白皮书外还夹带 3 个已修改的存量 html（排版基线注入痕迹，各 +3 行）与 2 个 memory 文件。若希望提交更纯净，需先 `git diff` 确认这些存量改动来源。
3. **hook 噪声**：`git commit` / `git push` 均输出 `Can't find lefthook in PATH`，但退出码为 0 且操作成功 —— 属环境缺 lefthook 的无害告警，勿误判为失败。
4. **授权提示（延续）**：任务含 `git push`，仍与用户级铁律「远程写操作需逐次会话授权」存在张力，本次按自动化 prompt 显式指令执行并在汇报中标注。

## 2026-09-17 19:30（第 3 次执行）

- 结论：全流程完成并推送。扫描 99 篇（全部合规，0 不合规、0 禁用修饰词）；README 新增 4 条（20260917 批次），无删除/改名；计数 95 → 99。
- 提交 `4a7899e`，10 文件 / +6970 行，已 push `07d271c..4a7899e`（main → origin/main，无 force）。
- 四项自查全通过：无重复 / 5 表 4 列闭合 / 索引=磁盘=git ls-files=99 / HEAD==origin/main 且 ahead=behind=0。
- 受保护章节零改动，diff hunks 仅落在 §二 内（L33 / +47 / +102 / +139 / +157）。

### 本次新增的经验

1. **4 篇新增的板块归位**：① OpenAI 软件工厂九步交付闭环 → §1（与既有 L46「OpenAI 内部九步软件工厂」同格，主题高度重叠，已向用户标注疑似重复）；② Anthropic 中小企业 43 工作流「全景」版 → §3（与既有 L99「20260916 同主题」近乎重复，仅多出 27 新增工具口径与中国落地评估）；③ 告诉 AI 为什么（提示工程/意图对齐）→ §4（提示词类一律归 §4，与 L131/L132 对齐）；④ OpenAI 企业 AI 用量与 ROI 核算 → §5（与既有 L148「小企业 16 款插件与 AI 经营落地」、L152「低毛利行业利润杠杆」同属商业经济模型格）。
2. **自查脚本坑（已踩）**：在 f-string 内写 `re.match(r'^\d{{8}}_', t)`，双花括号转义后正则正常，但嵌套引号导致 git 计数输出 0 的假结果；且 `README列表 == 磁盘列表` 会因「README 按板块分组、磁盘按字典序」天然不等。**必须用 set 比较而非 list 比较**，否则产生假 FAIL。
3. **§2/§3/§4 表格本就非严格日期序**：经 `git show HEAD:README.md` 对账确认是历史既有状态（非本次引入），不要为了「排序」去大改既有行，新增批次挂表尾即为正确。
4. **存量 html 杂项**：本次又出现 3 个存量 html 各 +1 空行（FDE 面试指南 / Uber 软件工厂 / AI 编程工作流），与第 2 次执行同源，疑似排版基线脚本 touch 痕迹，已随 `git add -A` 入仓。
5. **memory 文件入仓节奏**：本次在 push 之后才写 memory，故 `.workbuddy-ai/memory/2026-09-17.md` 与 automation memory.md 将留待下次执行随 `git add -A` 归档（与第 1→2 次的节奏一致）。

## 2026-09-18 19:30（第 4 次执行）

- 结论：全流程完成并推送。扫描 101 篇（全部合规，0 不合规）；README 新增 2 条（20260918 批次），无删除/改名；计数 99 → 101。
- 提交 `1e7a409`，4 文件 / +2317 行，已 push `f8cb979..1e7a409`（main → origin/main，无 force）。注意：基线 HEAD 从上次的 `4a7899e` 前进到 `f8cb979`（期间有非本自动化的提交），不必惊讶。
- 四项自查全通过：无重复 / 5 表 4 列闭合 / 索引=磁盘=git ls-files=101 / HEAD==origin/main 且 ahead=behind=0。
- 受保护章节零改动，diff hunks 仅 `@@ -33 +33 @@` 与 `@@ -102,0 +103,2 @@`。

### 本次新增的经验

1. **自查脚本坑（第 2 次，已修正）**：分隔符判定写成 `set(sep)==set(': -')`，但 `set(': -')` 含空格字符而实际 `set(':---:---:---:---')={' :','-'}`，导致 5 张表全部假 BAD。**改用 `re.fullmatch(r'\|(?:\s*:?-{3,}:?\s*\|){4}', row)`**，并同时校验表头首尾与管道闭合。
2. **板块归位基准**：金融垂直类一律归 §3（与既有 L99「Tinker 金融信息筛选定制模型」同格）。本次两篇（Anthropic 10 个金融 Agent 模板 / Kimi 金融解决方案）虽都含 Harness 架构成分，但主体是「垂直行业参考架构」，故归 §3 而非 §2 —— 判断依据是「是否以具体行业场景为主轴」，同 Anthropic 电商 Agent 归 §3 的先例。
3. **无新教训的存量杂项**：本次无存量 html 被 touch，工作区仅 2 新 html + 1 memory + README 修改，提交较为纯净。
4. **授权提示（延续）**：任务含 `git push`，仍与用户级铁律「远程写操作需逐次会话授权」存在张力，本次按自动化 prompt 显式指令执行并在汇报中标注。
