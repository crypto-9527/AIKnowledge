# 2026-09-14 工作日志

## 多 Agent 研发十原则文档：附件缺失 → 复用已有成稿

- 用户请求转换 `/Users/yangkejin/Downloads/frontier-engineering.md`（标题：从换工具到改写工作方式 · 面向多 Agent 研发实践的 10 条原则）。
- **源文件在磁盘上不存在**：Read 报 File does not exist，`ls ~/Downloads` 仅有 2 个 xlsx + 2 个 html，Spotlight `mdfind -name "frontier-engineering"` 与全文检索 `改写工作方式` 均无命中。用户 @ 引用的附件未落盘。
- 全文检索发现知识库已有 `20260910_多Agent研发实践十条原则与工程范式.html`，`<title>` 与需求标题**逐字匹配**，判定为同源文档的既有转换稿（19 章节 / 11 表格 / 13 滑块 / 12 SVG）。
- 经 AskUserQuestion 确认，用户选择**沿用 20260910 已有成稿**，不新建文件。

### 复核结果（三层全绿）
1. `validate_html.py` → 8/8 通过（105 个 ID，锚点 100% 可达，零外部依赖）。
2. `runtime_smoke.js` → PASS，ids=105，异常槽位=0。
3. `check_js_dom_bindings.py` → JS 引用 65 个 id，缺失 0。
- 附加确认：`<html lang="zh-CN" data-theme="dark">` 默认深色合规（§8.6）。

### 经验沉淀
- **附件路径不可信原则**：用户 `@` 引用或 `<file path>` 给出的路径必须先 `Read` 验证再动手，不能假定存在。
- **缺失时的取证顺序**：Read → `ls` 目标目录 → `mdfind -name` → `mdfind "kMDItemTextContent == '*关键词*'"` 全文检索知识库。本次靠最后一步发现同源成稿，避免重造。
- 发现同源成稿后应**先跑三层校验再向用户提议复用**，让用户基于「已验证全绿」的事实做决策，而非基于猜测。

## README 索引刷新与资产归档提交 (2026-09-14)

- **扫描盘点**：根目录实际收录 53 篇合规单文件 HTML 白皮书。
- **新增条目补充**：比对确认新增 `20260914_Meta个人AI代理Muse云端电脑与安全边界解析.html`，提炼核心理论（专属云端 Linux 电脑长程推进、模型非 Root 架构、替身凭证 / Sentinel 哨兵机制、eBPF 污点追踪防审批疲劳、单次虚拟卡支付防线）与关键机制（Feed / Ideas / Goals 三层分流、工作状态实时可见性、邮件 OTP 拦截与硬审批闭环），归入板块 2「多智能体架构与调度治理」。
- **全景矩阵同步**：README 知识资产全景矩阵总览计数更新至 53 篇，板块 2 由 14 篇扩展至 15 篇。
- **质量核验闭环**：
  1. `validate_html.py` 静态 8 项全绿通过。
  2. `runtime_smoke.js` DOM 运行时冒烟 PASS，26 个 ID 异常槽位=0，主题切换正常。
  3. 全景矩阵 53 行数据每行严格 4 列，所有条目链接与磁盘文件 100% 双向一致，0 悬空，0 遗漏。
- **Git 暂存与同步**：按 Conventional Commits 规范全量暂存并提交，推送到 origin main。
