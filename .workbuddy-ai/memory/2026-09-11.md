# 2026-09-11 工作日志

## 交付：苹果 2026 秋季发布会全景解读单文件 HTML

- **输入**：`/Users/yangkejin/Downloads/apple-september-event-2026.md`（Markdown，含 12 图 + 7 视频 + 7 VTT 字幕引用，以及一个 `<!-- component:device-map -->` 自定义组件占位）
- **产出**：`/Users/yangkejin/Documents/Openai/AIKnowledge/20260911_苹果2026秋季发布会五款硬件与AppleIntelligence分层承接.html`
  - 111,241 字节 / 1,657 行 / sha256 前缀 `f9ce8cdf`，零外部依赖、默认深色、侧栏贴左 + 正文居中
  - 结构：hero + 四条要点 + 口径说明 → 官方规格对账表（6 列 × 5 行）→ 7 个 H2 / 22 个 H3 完整承接原文 → 交互推演沙盘 → 页脚
  - 新增可视化：SVG 图 1「五款硬件 × AI 角色 × 算力归属」、SVG 图 2「Apple Intelligence 三层算力分流 + 三条硬边界」、光圈四档卡组、Watch 传感指标卡组、AirPods 双版本对照表
  - 新增交互沙盘 3 组（全部数据取自原文，无外部推算）：A 官方规格对账器、B 端侧门槛自检器、C 购买决策过滤器

### 关键决策与处理

1. **媒体资源缺失的裁决**：原文 `src="/media/apple-september-event-2026/…"` 为相对路径且附件未附带实体文件。为满足「零外部依赖 + 断网可用」，转为**媒体资产卡**，完整保留原始资源路径（含 7 条 `.zh-CN.vtt` 字幕轨道）、替代文本与图注。经脚本核对 **26/26 项媒体引用 100% 保留**。
2. **`<!-- component:device-map -->`**：按占位意图渲染为真实内联 SVG 设备分工图，数据严格取自原文第 1 节。
3. **`&#36;` 转义**：全文美元符号统一用 HTML 实体书写，既保证渲染为 `$`，又规避自检第 8 项对 `$...$` 成对匹配的货币误判。
4. **内容保真度实证**：对 170 个源内容块做前缀比对，23 处「未命中」经逐项核验**全部为审计口径假阳性**（编号风格 `1.`→`一 ·`、列表被拆为卡片、`<ol>` 序号由 CSS 生成等），无真实内容丢失。

### 验证记录（四道关卡全绿 + 反证）

| 关卡 | 结果 |
| :--- | :--- |
| 8 项静态自检 | 全部通过，Exit 0 |
| JS→DOM 反向绑定 | 27 个引用 id，0 缺失 |
| skill 自带运行时冒烟 | PASS，68 ids，异常槽位 0 |
| 带断言冒烟（自建） | PASS=59 FAIL=0（含 14 个 `out-*` 槽位静态初值 vs JS 重算值逐槽比对） |
| **反证（mutation counter-test）** | 变异 1（静态初值改错）→ FAIL 1 项；变异 2（JS 阈值写反）→ FAIL 3 项；原件全绿。证明测试具备真实检出能力 |

- 渲染级视觉核验（headless Chrome + PIL 切片）覆盖整页 0–13000px 与尾段 6520px，两张 SVG、全部表格/卡组/沙盘/页脚渲染无溢出、无压线、无重叠。

### 已修复的 skill 工具链缺陷（同步至三处副本）

1. **`sync_skill.py` 的 GLOBAL 目标早已失效**：脚本只同步 `~/.workbuddy/skills/`，而 Skill 工具实际从 **`~/.workbuddy-ai/skills/`** 加载（两者 SKILL.md 曾分别为 43167 / 40776 字节，漂移长期未被发现）。已扩展为三目标同步并重命名标签，`--apply` 后三处一致。
2. SKILL.md §6 新增两条：**保留前缀共七个**（`val-`/`sim-`/`res-`/`out-`/`stat-`/`usl-`/`pf-`），静态容器命名须全部避开；**同文件并行 Edit 会静默丢弃**（三个调用全报成功但只有第一个落地）。
3. SKILL.md Stage 5.5 ⑥ 新增两条：长文档超出单张截图内存上限（13000px 可行 / 17000px 必失败）时的**切片渲染法**；zsh `rm -rf po*` 无匹配会中止整个复合命令、以及 `pkill` pattern 必须带 `headless` 以免误杀用户自己的 Chrome。

## 交付：crypto-ai-qt 有状态量化 Agent 架构选型与落地方案

- **输入**：`/Users/yangkejin/Downloads/crypto-ai-qt_有状态量化Agent架构选型与落地方案_20260909.html`
- **产出**：`/Users/yangkejin/Documents/Openai/AIKnowledge/20260911_crypto-ai-qt有状态量化Agent架构选型与落地方案.html`
  - 52,794 字节 / 988 行，完全自包含、零外部依赖单文件 HTML
  - 结构保真：完整继承 17 个 Section、顶部 Hero、Ribbon 三大定调卡片、9 张对比/规划表格、23 篇来源文献（S01-S23 完整引用及外链）、四平面泳道流水线（A/B/C/D 四平面及步骤分支）
  - 排版与视觉升级：遵循 AIKnowledge 黑曜石深色（默认）/ 暖羊皮纸浅色双主题基线，陶土橙强调色，衬线标题 + 无衬线正文 + 等宽代码栈，正文在侧栏右侧居中盒模型，顶置平滑阅读进度条
  - 交互功能：保留候选方案实时过滤器（`#candidate-filter`）、主题切换按钮（`#theme`）、打印按钮（`#print`）以及章节目录 IntersectionObserver 动态高亮
  - 质量门禁：8 项静态自检（`validate_html.py`）全部通过（Exit Code 0），DOM 反向绑定检查（`check_js_dom_bindings.py`）0 缺失，Node DOM 垫片运行时冒烟（`runtime_smoke.js`）PASS（48 IDs，0 异常）
