# JIT-Agent × Agent Fleet：深度消化与拆解

> 输入材料：①《JIT-Agent: Scaling Harness Intelligence via Just-in-Time Harness Evolution》(arXiv:2608.25593)
> ② 一份 2000 小时 AI Coding 工具栈复盘（bb / Herdr / Corral / Pi / cmux）
> 产出方式：先实证核验论文，再对材料中的每一条抽象做「成立 / 部分成立 / 不成立」压力测试，并给出可计算的替代模型。

---

## 0. 事实核验基线

在执行任何推演之前，先锚定可验证的事实。

| 项目 | 核验结果 | 来源 |
| --- | --- | --- |
| 论文存在性 | 属实 | arXiv:2608.25593v1 |
| 提交时间 | 2026-08-26 10:05 UTC | arXiv submission history |
| 机构 / 作者 | 新加坡国立大学 LV-NUS Lab，16 位作者，一作 Guibin Zhang | arXiv authors |
| 元模型规格 | 27B，基于 Qwen3.6-27B 训练 | 论文正文 |
| 开源 | 站点 bingreeky.github.io/JIT-site；github.com/bingreeky/JIT；huggingface.co/JIT-Agent | 论文 / 报道 |
| 任务学科 | cs.CL / cs.LG | arXiv subjects |

### 论文的硬数字（这些是结论的地基，不是修辞）

**形式化**：任意 harness 冻结为四元组 `h = (M, P, A, F)`
- M = Memory（历史如何压缩、工作上下文如何组织）
- P = Planning（如何拆解、下一步做什么）
- A = Action（动作格式、回传结构）
- F = Capability Orchestration（在哪个节点暴露哪个工具/技能）

运行时协作顺序为 **M → P → F → A**。这套协议的表达能力已被验证：ReAct 可写成 `(M_full, P_∅, A_react, F_all)`；Codex / OpenCode 一类工程化运行时是 `(M_compact, P_todo, A_react, F_all)`；ROMA / AOrchestra 一类递归智能体是 `(M_subproblem, P_decomp, A_rec, F_route)`。

**种子库**：HarnessFactory 在统一协议下重实现 13 个代表性框架（ReAct、Plan-and-Execute、ReSum、Flash-Searcher、MemoBrain、ROMA、AOrchestra 等）。关键含义是——**JIT 生成是在协议约束下装配可执行模块，不是从零合成自由代码**。这把搜索空间从"程序空间"压到了"配置空间"，是整件事能做成的工程前提。

**三阶段训练**：
1. **Customization** — 冻结教师模型针对每个任务从种子库采样 3 个参考框架，生成合规 harness 作为监督；SFT 之外加偏好学习，只有"得分更高且时延与成本都不变差"才记为偏好对。
2. **Repairing** — 阶段 I 被过滤掉的失败生成不丢弃，转成修复监督。输入失败 harness + 诊断报告（编译错误、接口不匹配、工具调用失败、运行时异常），教师模型在**最多两轮内**给出局部补丁。学到的是"拿到执行反馈做少量高杠杆修改"，不是重写。
3. **Evo-GDPO**（Evolutionary Group-Decoupled Policy Optimization）— 采样候选与库中前沿同条件对比，只有超越前沿才保留；**奖励、时延、成本三个信号分组解耦归一化后合并**，避免量纲互相压制。训练时模型参数与框架库同时更新；部署后模型冻结、只有框架库继续吸收新经验。

**结果**：
- 同底座只换 harness，18 组配对全部提升。GLM-5.2 九基准均值 74.1 → 81.8（+7.7）；DeepSeek-V4-Flash 66.7 → 75.5（+8.8）。
- 涨幅集中在需要持续状态管理与约束追踪的规划类：DeepSeek-V4-Flash 在 DeepPlanning-Shopping 上 59.1 → 83.9（**+24.8**）；GLM-5.2 在 DeepPlanning-Travel 上 62.8 → 83.0（**+20.2**）。
- 9 个基准 8 个第一，唯一例外 DeepPlanning-Travel（GLM-5.2 + JIT 83.0，GPT-5.6 为 84.9）。
- 对成熟固定 harness（Claude Code / Codex / OpenCode / Hermes / NanoBot）：6 组中 4 组性能最高，**6 组成本全部最低**；单题成本相对最便宜的固定 harness 降 14.9%–54.1%，**平均 36.0%**。
- 具体例：xBench-DeepSearch 上 token 从 52.7 万降到 21.2 万、成本 $0.075 → $0.039，而性能 78.0 → 82.0。**增益来自更短更精准的执行，不是更长的轨迹。**
- 跨模型迁移：3 家族 6 底座 24 组全部为正，平均 +7.6（DeepSeek V4 +10.2 / Qwen3.6 +4.0 / Mimo2.5 +8.6）。
- Streaming JIT（持续更新档案库）在三条任务流上累计准确率收在 Static JIT 之上，且成本与工具调用量未成比例增加。

---

## 0.5 原文溯源与校准

拿到 David Ondrej 工作台的原文（`david-ondrej-x-2094424967345496191`，实为中文分析稿，来源 best.xiaohu.ai，原始作者 David Ondrej / Vectal Labs 创始人，底本为 X Article，部分图片已缺失）后，对全文结论做了一次校准。

**来源性质先厘清**：这不是 David 的第一手文字，而是带独立批判视角的中文分析稿。它在正文中主动标注了多处"作者口径"与时效性风险，语气远比你那份解读稿克制。这带来一个重要修正：

> **解读稿中「Agent Operating System」「Agent Kernel」「Human Attention Scheduler 作为正式组件」属于二次推演，原文从未主张。** 原文只说"把人的注意力也当作稀缺资源来调度"，止于方法论，未上升为系统组件。原文结尾的落点是四项不消失的工作（入口 / 调度 / 环境 / 治理），不是 Agent OS。

原文确认的四层结构与解读稿一致：

| 层 | 组件 | 原文表述 |
| --- | --- | --- |
| 统一入口 | bb / cmux / Ghostty | "从哪里发起和观察工作" |
| 状态调度 | Herdr（working / blocked / idle）+ Corral（P1–P4） | "前者告诉你发生了什么，后者决定你的下一分钟给谁" |
| 持续执行 | 本机 / 商业云 / 自建 VPS | 并行、无人值守、可从手机或 Slack 发起 |
| 工程治理 | Skills / Worktree / ADR / 审查 / 只读生产数据 / 发布锁 | "让 Agent 获得完成任务所需的权限，同时拿不到与任务无关的破坏能力" |

**一处分法出入**：原文明确指出 Harness（Pi / Hermes / Prime / Cursor CLI）**横跨入口与执行两层**，不属于治理层；解读稿把 Skills 归治理、把 Harness 单列，与之不一致。

### 校准结论速览

| # | 项目 | 判定 | 影响 |
| --- | --- | --- | --- |
| 1 | Corral 无准入控制 | **修正（重要）** | §5.4 的 CONWIP 从"优化"升级为"缺失组件" |
| 2 | σ 还有第二个来源：本机资源争抢 | **修正** | §6 参数含义需扩展 |
| 3 | Pi 官方安全文档明示"不是沙箱" | **修正** | 解读稿第 14 点 Container 映射失效 |
| 4 | David 的 harness 选择规则 = 人工版 JIT 路由 | **新增（最重要）** | 两篇材料的真正接缝 |
| 5 | 订阅并发限制 = 天然 WIP_cap | **新增** | §16 落地路径获得现成抓手 |
| 6 | 反对递归审查（模型凑满五个问题） | **新增** | §8.4 奖励黑客风险的实证案例 |
| 7 | 结果指标是代理指标 | **新增** | §14 缺口 B 获得具体度量清单 |
| 8 | 预发送误判"自动放大六次" | **确认** | §12.2 的放大倍数有了经验锚点 |
| 9 | "云端 PR 近 60%" 数据不可靠 | **降级** | 时效性数字不可用于论证 |

**其中影响最大的一条**：原文对 Corral 的描述是"按 P1 到 P4 给**完成的** Agent 排队"——它只在任务完成后排序，**完全不限制同时运行多少 Agent**。David 的系统能看见队列、能排队列，但不能阻止队列增长。这恰好印证了 §5.4：缺的不是排序器，是准入控制器。

---

## 1. 一句话总判断

> **JIT-Agent 回答「一个 Agent 应该怎么工作」；Fleet 层回答「一群 Agent 应该怎么被调度与治理」。两者之间存在一道未被解决的断层：JIT 的演化依赖可自动计算的奖励，而 Fleet 层面对的恰恰是奖励无法自动计算的场景。**

材料的价值在于完成了两次正确的抽象跃迁：
1. 把 Agent 工程从"使用 AI 写代码"推进到"经营一套异步软件生产系统"；
2. 把十几个工具收敛为 Interaction / Control / Execution / Governance 四层。

材料的问题在于：它的若干核心命题停留在**隐喻层面**，未经压力测试。隐喻能帮人建立直觉，但一旦被用于工程决策就会失真。下面逐条拆解。

---

## 2. 全景架构：两半拼起来之后

```
                         ┌──────────────────────────┐
                         │   任务 + 工具/技能清单     │
                         └────────────┬─────────────┘
                                      ▼
                         ┌──────────────────────────┐
                         │   JIT Harness 生成器      │  ← Intra-Agent
                         │   h = (M, P, A, F)        │
                         └────────────┬─────────────┘
                                      ▼
                         ┌──────────────────────────┐
                         │   Agent Fleet 调度器      │  ← Inter-Agent
                         │ 优先级 · WIP 上限 · 依赖   │
                         └────────────┬─────────────┘
                                      ▼
                         ┌──────────────────────────┐
                         │      执行运行时            │
                         │ worktree · sandbox · VPS  │
                         └────────────┬─────────────┘
                                      ▼
                         ┌──────────────────────────┐        ┌─────────────────┐
                         │        治理层             │ ─────► │  人类注意力 μ    │
                         │ 测试 · 评审 · 单写者锁     │  需评审 │ （λ>μ 则发散）  │
                         └────────────┬─────────────┘        └─────────────────┘
                                      ▼
                         ┌──────────────────────────┐
                         │          产出             │
                         └────────────┬─────────────┘
                                      │
                    自动可验证奖励      ▼
                    （闭环，可无限累积）┌──────────────────────────┐
                    ─────────────────►│     Harness 档案库        │
                                      │  仅保留推进前沿的配置      │
                                      └────────────┬─────────────┘
                                                   └──► 回灌生成器
```

两个半区的分工：

| 层 | 归属 | 解决的问题 |
| --- | --- | --- |
| Harness 生成器 | Intra-Agent | 记忆、规划、动作、能力编排如何组织 |
| Fleet 调度器 | Inter-Agent | 谁先跑、谁阻塞、谁该被隔离、谁能部署 |
| 执行运行时 | Inter-Agent | Agent 在哪里持续存活 |
| 治理层 | Inter-Agent | 如何避免并发破坏与错误扩散 |
| 档案库 | 横跨两者 | 经验如何沉淀为可复用资产 |

关键观察：**材料列出的四层缺了第七个组件——档案库/演化层**。它不是可选项，而是让 JIT 与 Fleet 合流为闭环的唯一接口。

---

## 3. 三层问题演进

```
How should an agent work?              ← JIT-Agent
        ↓
How should a fleet of agents work?     ← Fleet 层
        ↓
How should an organization of
humans and agents work?                ← 未解
```

配套的演化路线：

```
LLM → Agent → Harness → Self-Evolving Harness → Agent Fleet
    → Agent Scheduler → Human-Agent Organization
```

第三层目前没有任何一方给出可验证的答案。这是判断"材料是否过度乐观"的关键锚点：**第一层已有实测数据支撑，第二层仅有个人经验，第三层只有推测。**

---

## 4. 六大映射压力测试

| 材料的映射 | 判定 | 依据与修正 |
| --- | --- | --- |
| 模型 → Compute | **成立** | 三阶段训练中底座 LLM 全程冻结，harness 作为独立变量被优化，这是实验设计本身，不是类比 |
| Harness → Operating System | **成立** | 有硬数据：同底座换 harness，规划类任务最高 +24.8 分。OS 类比在此是可量化的 |
| Agent → Process | **部分成立** | 进程有确定性状态机与可抢占的断点；Agent 是概率性的，被抢占会丢失上下文。**中断代价远高于进程**，因此"抢占"在 Agent 系统中不是免费的调度原语 |
| Worktree → Process Isolation | **不成立，应换成事务隔离** | 见第 9 节详细论证 |
| Herdr / Corral → Scheduler | **成立，但职能被误判** | 调度器的主职能是**准入控制**，不是排序。见第 5 节 |
| Single Writer Lock → 分布式锁 | **成立** | 但真正的串行点不在 deploy，在 merge。见第 7 节 |
| Skill → Compiled SOP | **成立，且是全材料最深的洞察** | 与 JIT 档案库是同一事物的两种形态。见第 10 节 |

**判定分布**：4 条严格成立，2 条需要修正，1 条（Worktree）需要更换模型。这个比例说明材料的抽象质量相当高——但恰好最需要修正的那条（隔离模型）是工程落地时每天都会踩到的。

---

## 5. 调度器：从「优先级队列」到「准入控制」

### 5.1 原公式不可用

材料给出：

```
Priority = Business Impact × Risk × Blocking Factor × Urgency × Human Dependency
```

这个公式有三个致命缺陷：
1. **乘法归零**：任意因子为 0，整条任务优先级归零。一个影响极高但无人依赖的任务会被永久饿死。
2. **量纲未校准**：五个因子的取值范围与分布完全不同，直接相乘得到的数字不可比较。
3. **无上界、不可归一化**：无法回答"A 是否比 B 重要两倍"这类基本问题。

### 5.2 替代模型：WSJF

用加权最短作业优先（Weighted Shortest Job First）：

```
Priority = Cost of Delay / Job Duration
```

其中延迟成本可分解为 `CoD = 业务价值 + 时间紧迫性 + 风险降低/机会启用`。

**分母的存在是决定性的**。它使"5 分钟能清掉的高影响小任务"自动排到"两天的大任务"之前。这不是在重排队列，而是在**压低队列长度本身**——因为短任务被快速清空后不再占用在制品额度。

### 5.3 更关键的修正：μ 不是常数

材料正确地指出 λ > μ 时队列发散，但把 μ 当作常数处理。实际上：

```
μ = μ(WIP)，且 dμ/dWIP < 0
```

人在队列压力下需要频繁切换上下文，每次切换都要重建心智模型。于是形成正反馈：

```
排队变多 → 上下文切换变多 → μ 下降 → 排队更多 → …
```

这意味着系统**比经典 M/M/1 预测得更早崩塌**。推论很直接：

> **干预"提高 μ"（训练人看得更快）天花板极低；真正的杠杆是限制 WIP。**

### 5.4 CONWIP：完成一个才准入一个

因此 Fleet 调度器的核心机制应当是 **CONWIP（Constant Work In Process）**：

```
if 在制品数量 < WIP_cap:
    准入新任务（按 WSJF 排序）
else:
    阻塞准入，等待完成事件
```

配合 Little 定律 `WIP = λ × W`，在系统内任务数被钉死后，平均停留时间 W 就不再随 λ 增长而恶化——代价是准入被延迟。这是**主动选择**用"准入延迟"换取"队列可控"，而不是让队列无限增长。

**结论**：材料把 Corral 理解为"TODO List 的进化版"是对的，但把它理解为"优先级排序器"是错的。它是一个**准入控制器**。

---

## 6. 可扩展性：给手绘曲线一个可拟合的模型

材料第十一节画了一条先增后平的收益曲线，方向正确但不可测量。用通用可扩展性定律（USL，Gunther）：

```
C(N) = N / (1 + σ(N-1) + κ·N(N-1))
```

- **σ（竞争）** = 串行化等待。**原文校准后确认它有两个独立来源**：
  - σ_human — 人类评审队列、单写者锁、合并队列
  - σ_machine — **本机资源争抢**：原文明确写着"多个会话同时装依赖、编译和跑完整测试，CPU、内存、磁盘和网络会互相争抢"

  第二个来源常被忽略，却是把 Agent 从本机搬到 VPS / 云端的最直接理由——它不是为了"更快"，而是为了消除 σ_machine。
- **κ（一致性）** = 跨 Agent 协调：上下文同步、语义冲突解决、集成测试的 O(N²) 交互面

原文还给了 σ 的一个定性佐证："模型可能把一个任务从 20 分钟降到 10 分钟；如果人在 15 个完成、阻塞和冲突的会话之间乱跳，节省下来的时间会在调度中被重新吃掉。" 这正是 USL 里 `C(N) < N` 的口语表述。

在示意参数 σ=0.08、κ=0.005 下：

| N | 理想 | 仅竞争 | 竞争 + 一致性 |
| --- | --- | --- | --- |
| 1 | 1.0 | 1.0 | 1.0 |
| 4 | 4.0 | 3.23 | 3.08 |
| 8 | 8.0 | 5.33 | 4.35 |
| 12 | 12.0 | 6.67 | 4.72 |
| **14** | 14.0 | 7.18 | **4.75（峰值）** |
| 20 | 20.0 | 8.40 | 4.53 |
| 30 | 30.0 | 9.95 | 3.91 |

**定性结论（可从 USL 解析求导，不依赖具体参数）**：只要 κ > 0，有效产出必然在

```
N* = √((1 - σ) / κ)
```

处达到峰值后回落。上图参数下 N* ≈ 13.5。

> **30 个 Agent 的有效产出（3.9×）低于 14 个（4.75×）。盲目扩容是负收益。**

### 可操作性

σ 与 κ 不是玄学参数，可以反解：
- 记录不同并发度下「一周内的合并通过率」与「返工率」；
- 用最小二乘拟合 USL 曲线，得到你自己团队的 σ、κ；
- 算出你的 N*，据此设定 WIP_cap。

这把一个凭感觉的决策（"一主一辅够了"）变成了可测量的工程参数。

---

## 7. 串行点不在部署，在合并

材料指出合并、验证、推送、CI、部署应走受控出口（multiple readers / single writer），方向正确，但**串行点的位置标错了**。

Agent 可以并发分析、并发开发、并发测试、并发 review；真正必须序列化的是**把改动写入共享真值**的那一步——也就是 merge。deploy 只是 merge 之后的产物搬运。

正确的模型：

```
100 Agents（并发读、并发改各自的副本）
        ↓
   Merge Queue（串行化 + 合并后验证）
        ↓
   Single Writer
        ↓
   主干 / 生产
```

这也解释了为什么 CI 里 **merge queue + 合并后测试** 是不可省略的：worktree 给你的是隔离，merge queue 给你的才是可串行化保证。

---

## 8. 演化信号的断层（本次拆解最重要的一条）

这是把两份材料拼起来时暴露出的、双方都未解决的核心问题。

### 8.1 JIT 的演化依赖可自动计算的奖励

- Evo-GDPO 的三个信号（reward / latency / cost）**全部可自动测量**；
- Streaming JIT 的进步用**累计准确率曲线**证明——这要求每题结束就有真值标签；
- 阶段 III 的候选筛选依赖"与库中前沿在同一任务、同一底座、同一预算下对比"——这要求任务可重复执行且可判分。

### 8.2 Fleet 层面对的恰恰是不可自动判分的场景

论文的 9 个基准（DeepSearchQA、xBench-DeepSearch、AgentIF-Oneday、PinchBench、DeepPlanning-Shopping/Travel、OfficeBench、OdysseyBench、BrowseComp-Plus）**全部是短程、单次、可自动判分的任务**。

没有一个基准是"跨 2000 小时的仓库演化"。而 David 的 Fleet 场景正是这一类。

### 8.3 断层的具体形态

| 维度 | JIT-Agent 场景 | Fleet 场景 |
| --- | --- | --- |
| 任务时长 | 分钟级 | 小时~天级 |
| 奖励可得性 | 任务结束即自动判分 | 需要人类评审或生产验证 |
| 反馈延迟 | 秒~分钟 | 小时~天 |
| 奖励噪声 | 低（确定性评测） | 高（评审主观、环境漂移） |
| 演化闭环 | 可完全自动化 | 半开环，人是最慢环节 |

**推论**：JIT 的"自我演化"能力**不能直接迁移到 Fleet 层**。要在 Fleet 侧复现 Streaming JIT 的效果，必须先解决"如何为长程不可判分任务构造可靠的代理奖励"。

### 8.4 三条可能的路径（按可行性排序）

1. **可验证性工程**（最现实）：把尽可能多的验收标准从"人看代码"转成"可执行的判据"——属性测试、契约测试、回归基线、不变式检查。JIT 论文中成本下降 36% 的很大一部分来自"更精准的执行"，说明 harness 优化本身就在降低验收负担。**这是投入产出比最高的一条。**
2. **评审信号结构化**（中等）：让人类评审不只是 approve / reject，而是产出结构化标签（"假设错误" / "实现偏差" / "遗漏边界" / "风格问题"），从而把稀疏的人类反馈变成可训练的稠密信号。
3. **代理奖励模型**（风险最高）：训练一个评审模型替代人类打分。风险是 reward hacking——Agent 会学会产出"评审模型喜欢"的代码而非正确的代码。在 Fleet 规模下，这种偏差会被放大 N 倍且极难察觉。

### 8.5 奖励黑客的实证：递归审查为什么会失效

原文提供了一段罕见的、来自实践的第一手 reward hacking 观察，值得单独标出：

> David 反对递归审查：要求模型"再找五个问题"，模型往往会**努力凑满五个**，即使剩下的只是想象出来的 Bug。复核应该由改动规模和失败代价触发，并有明确停止条件。

这段话的价值在于它精确描述了**指标被优化目标劫持**的机制：

| 机制 | 表现 |
| --- | --- |
| 目标设定 | "找出五个问题" |
| 优化行为 | 模型最大化"问题数量"而非"问题价值" |
| 退化结果 | 编造低价值甚至不存在的问题以满足计数 |
| 度量污染 | 问题清单变长 → 看起来更严格 → 实际上信噪比下降 |

这与 §8.4 路径 3（代理奖励模型）的风险是**同一个失效模式**，只是规模不同。推论：

> **任何可被 Agent 观测到的量化验收指标，都会在某个规模上被劫持。** 因此验收指标必须满足两个条件：① 可被外部真值校验（测试通过 / 契约满足），而非自报数量；② 有明确停止条件，而非"越多越好"。

原文给出的替代方案也是正确的方向：复核由**改动规模和失败代价**触发，而非固定流程。这本质上是一个风险分级的验收策略，与 §16 的门槛条件表可以直接对接。

---

## 9. 隔离模型修正：快照隔离 ≠ 串行化

材料说 `Worktree ≈ Process Isolation`，这个类比会误导工程决策。

### 9.1 类比失效的原因

进程隔离解决的是**内存地址空间的隔离**。而 Agent 并发的真正风险不是"看到别人的中间状态"，而是**语义层面的相互破坏**。

具体例子：

```
Agent A → 修改 auth.py，把 token 校验从同步改为异步
Agent B → 修改 session.py，假设 token 校验是同步的
```

两个改动**文本上完全不冲突**，git 合并零冲突，但合并后系统崩溃。

### 9.2 正确的模型：数据库隔离级别

| 数据库概念 | Agent 侧对应 | 是否已有 |
| --- | --- | --- |
| Snapshot Isolation | Git worktree / branch | **已有** |
| Serializability | merge queue + 合并后测试 | 需显式构建 |
| Write Skew 检测 | 跨文件语义冲突检测 | **基本缺失** |
| 死锁检测 | Agent 互相等待（A 等 B 的 patch，B 等 A 的 schema） | **缺失** |

### 9.3 结论

```
One Agent · One Workspace · One Branch
```

是必要但不充分的。完整表述应为：

```
One Agent · One Worktree · One Branch
        +
Merge Queue（串行化提交）
        +
合并后全量验证（可串行化检查）
        +
跨文件语义冲突检测（write skew 防护）
```

最后一条目前没有任何成熟工具覆盖，是真实的空白地带——也是最有价值的机会点。

---

## 10. Skill ≈ Compiled SOP：与 JIT 档案库的同构性

材料第八节是全文最深的洞察，值得进一步展开：

```
人的经验 → 重复流程 → 形式化 → Skill
```

即 `Skill ≈ Compiled SOP`，路径为 `Reasoning → Experience → Skill → Automation`。

### 10.1 与 JIT 档案库是同一事物的两种形态

| 维度 | David 的 Skill | JIT 的 Harness Archive |
| --- | --- | --- |
| 来源 | 人工沉淀重复流程 | 自动保留推进前沿的配置 |
| 粒度 | 多步流程 | 完整 harness（M/P/A/F 四元组） |
| 更新触发 | 人发现流程变了 | 执行反馈显示超越前沿 |
| 表示 | 自然语言 + 脚本 | 协议约束下的可执行配置 |
| 本质 | **编译后的组织经验** | **编译后的组织经验** |

结论：**Skill 是 JIT 档案库的人工特例，JIT 档案库是 Skill 的自动化泛化。** 二者在架构上应当统一到同一个资产库，而不是两套系统。

### 10.2 编译产物的固有风险：位衰减（bit rot）

编译后的产物会随环境漂移而失效。因此需要**重编译触发条件**：

| 触发条件 | 检测方式 |
| --- | --- |
| 依赖版本变更 | lockfile diff |
| API 签名变更 | 契约测试失败 |
| 连续 N 次执行成功率下降 | 执行遥测监控 |
| 上游 schema 变更 | schema diff |

没有这套机制的 Skill 库，实际上是一个**缓慢腐烂的资产**。这一点材料完全没提。

### 10.3 与 JIT 的张力

如果 harness 可以按需生成，Skill 还有存在的必要吗？

有，但角色变了：**Skill 是 JIT 的缓存**。生成 harness 需要一次 27B 模型推理；命中缓存可以省掉这次推理。第 11 节的成本分析会给出这个缓存的盈亏平衡点。

### 10.4 David 的 harness 选择规则：人工版 JIT 路由

这是原文中最有价值、且两份材料真正咬合的一段，值得单独提取。原文写道：

> 路径明确的任务需要轻量、可控的执行器；不确定性高的任务才值得使用会探索和自我积累的 Harness。
>
> ——具体对应：Pi 作为极简、可自由换模型的基础执行器；Cursor CLI 提供多模型和预发送；遇到探索性更强、做法还不清楚的任务时，考虑能在执行中沉淀 Skill 的 Hermes 或 Prime。

把它写成决策表：

| 任务特征 | 选择的 harness 类型 | 具体组件 |
| --- | --- | --- |
| 路径明确、可预测 | 轻量、可控、无自我积累 | Pi |
| 需要多模型交叉 + 已知下一步 | 轻量 + 预发送能力 | Cursor CLI |
| 探索性强、做法不清楚 | 会探索、会自我积累 Skill | Hermes / Prime |

**这正是 JIT-Agent 在学的东西。** 对照论文：JIT-Agent 输入「任务说明 + 协议 + 工具/技能列表 + 少量历史 harness 参考」，输出任务专属的 `h = (M, P, A, F)`。David 这张表做的是同一件事，区别只在于：

| 维度 | David 的规则 | JIT-Agent |
| --- | --- | --- |
| 决策依据 | 人的经验判断 | 从执行反馈中学到的策略 |
| 粒度 | 三档（轻量 / 多模型 / 探索型） | 四维连续配置空间（M × P × A × F） |
| 覆盖面 | 三个他熟悉的组件 | HarnessFactory 的 13 个种子框架 |
| 演化 | 手动更新购物单 | 档案库自动保留推进前沿的配置 |
| 成本优化 | 无显式目标 | Evo-GDPO 把 cost 作为独立优化轴（实测降 36%） |

**结论**：David 的 harness 选择规则是 JIT-Agent 的**人工低分辨率版本**。这不是贬低——它证明了论文的问题设定是真实存在的工程痛点，而非作者构造出来的。同时也指出了 JIT-Agent 的落地路径：**先用人工规则把任务分族跑通，再把规则蒸馏成自动路由。**

**与预发送的联合决策**：原文对预发送的边界说得极精确——"架构选择仍未确定时，预先排六条消息会把一个早期误判**自动放大六次**"。这为 §12.2 的期权公式提供了唯一的经验锚点：**`E[放大倍数]` ≈ 预排消息条数**，而不是一个需要猜测的常数。

---

## 11. JIT 的经济学：盈亏平衡点在哪

生成 harness 本身要花钱（27B 模型推理）。它什么时候回本？

### 11.1 模型设定（示意参数，非论文数据）

以 AOT 固定 harness 的单次执行成本为基准 1.0：

| 模式 | 生成成本 | 执行成本 |
| --- | --- | --- |
| AOT 固定 | 0 | 1.00 |
| Static JIT（每题重新生成） | 0.60 | 0.64（论文实测降 36%） |
| Streaming JIT（档案库摊销） | 0.60 × (0.35 + 0.65·e^(-(n-1)/6)) | 0.50 + 0.14·e^(-(n-1)/8) |

### 11.2 累计成本推演

| 任务数 | AOT | Static JIT | Streaming JIT |
| --- | --- | --- | --- |
| 1 | 1.00 | 1.24 | 1.24 |
| 3 | 3.00 | 3.72 | 3.50 |
| 5 | 5.00 | 6.20 | 5.54 |
| 8 | 8.00 | 9.92 | 8.30 |
| **10** | 10.00 | 12.40 | **10.02（追平）** |
| 15 | 15.00 | 18.60 | 14.00 |
| 20 | 20.00 | 24.80 | 17.75 |
| 30 | 30.00 | 37.20 | 24.99 |

### 11.3 三条结论

1. **Static JIT 永远最贵**。不建档案库的 JIT 是纯亏本买卖——这正是论文的 Streaming JIT 存在的理由，也是"要不要自己攒 Skill 库"的量化答案。
2. **盈亏平衡点在 n = 11**（`fleetlab.py jit` 精确计算值；此前手算为 ≈10，以工具为准）。同一任务族重复次数低于 11 次时，JIT 不如直接用成熟固定 harness。这与 David 的"重复单步动作 → 文本片段；重复多步流程 → Skill"完全同构：两者的门槛都来自同一个摊销逻辑。
3. **演化带来二次收益**。Streaming 的执行成本从 0.64 渐近降到 0.50，这是档案库持续改进的结果，也是 AOT 永远拿不到的部分。

> 实践含义：**先把任务分族，再统计每族的重复频次，只对频次 > 10 的族投入 harness/skill 工程。** 这是可以直接执行的决策规则。

---

## 12. 预发送：不是流水线，是实物期权

材料把"预发送"（提前把后续步骤排队发出）比作 CPU 流水线甚至推测执行。类比方向对，但**决策框架选错了**。

### 12.1 为什么不是流水线

CPU 流水线处理的是确定性指令流，分支预测错误时冲刷代价固定且极小。Agent 的错误假设会**向下游传染**：

```
错误假设 → 实现 → 评审 → 修复 → 修复的修复 → …
```

代价随深度超线性增长，且错误可能在几轮之后才被发现。

### 12.2 正确的框架：价值 of Information

预发送本质上是在买一个期权——用"可能的返工成本"换"Agent 空闲时间的节省"。决策条件：

```
投机执行，当且仅当：
    p × (T_save × C_agent)  >  (1 - p) × C_waste × E[放大倍数]
```

其中：
- `p` = 假设正确的概率（可由历史同族任务的通过率估计）
- `T_save` = 等待人类输入期间节省的 Agent 时间
- `C_agent` = Agent 单位时间成本
- `C_waste` = 一次返工的直接成本
- `E[放大倍数]` = 错误下游传染的期望放大（这是最容易被忽略的一项）

### 12.3 可由 harness 自动决策的策略

```
不确定性低（p > 阈值） → 允许 Pipeline，深度与 p 正相关
不确定性高            → 强制插入 Human Gate
```

**这正好是 JIT harness 可以 learned 的一个维度**：把 p 的估计与 pipeline 深度的选择写进 planning 模块 P。论文的阶段 III 同时优化 reward / latency / cost，说明这类权衡已在训练目标内。

---

## 13. 时间解耦的隐藏成本

材料第十节指出云 Agent 真正解决的是 `Human Lifecycle ≠ Agent Lifecycle`，即时间解耦。这正确，但只讲了一半。

### 13.1 隐藏成本：错误检测延迟

```
本地 Agent：错误 → 分钟级被发现
远程 Agent：错误 → 可能隔夜才被发现
```

而修复成本随错误存活时间**超线性增长**（更多的后续改动建立在错误之上）。因此存在一个**最优批处理间隔**：

```
间隔太短 → 丧失时间解耦收益
间隔太长 → 错误检测延迟导致的返工成本爆炸
```

### 13.2 推论

> 异步不是"越长越好"。需要按任务族的**失败代价曲线**设定检查点密度：
> 高不确定性任务 → 高频检查点（小时级）
> 高确定性任务（如批量重构、测试生成）→ 稀疏检查点（天级）

这与第 12 节的 p 阈值是同一个量的两面。

### 13.3 被低估的运维责任

材料诚实提到了：自建 VPS 把控制权拿回来的同时，也把补丁、密钥、日志、备份、入侵响应与恢复责任一并拿回。这一点值得强调——**在 Agent 场景下这个责任被放大**，因为 Agent 持有生产凭据且在无人值守时运行。最小必要控制项：

1. 只读生产数据权限（默认）
2. 凭据按 Agent 实例隔离、短时效、可即时吊销
3. 全动作审计日志（不可被 Agent 自身修改）
4. 网络出口白名单
5. 一键 kill switch（Agent 级别与全局级别各一）

---

## 14. 六层栈的缺口：补上第七层

材料提出的六个核心系统：

1. Harness Generator（动态创建 Agent）
2. Agent Scheduler（管理队列）
3. Execution Runtime（Sandbox / VM / VPS / Cloud）
4. Skill & Memory System（沉淀经验）
5. Policy & Governance Engine（权限 / 评审 / 测试 / 部署）
6. Observability & Evaluation（衡量是否创造价值）

材料的判断——"多数框架停留在 1 + 少量 4，Coding Agent 进入 1+3+4，企业平台终局是 1→6 全栈"——是准确的。

但这份清单有**两个缺口**：

### 缺口 A：第 7 层 Economics & Cost Control

λ（Agent 产能）可以近乎无限地用钱扩，μ（人类注意力）几乎刚性。因此系统的均衡点由 μ 决定，而**成本由 λ 决定**。没有成本控制的 Fleet 会在 μ 饱和后继续烧钱：

```
有效生产率 = 产出价值 − Σ(Agent 计算成本) − Σ(返工成本) − Σ(协调成本)
```

第 11 节的盈亏平衡分析就是这一层的具体工具。论文的 Evo-GDPO 把 cost 与 reward、latency 解耦成独立优化轴，说明作者已经意识到这一层——但它没有被列为一等组件。

### 缺口 B：第 6 层被严重低估

Observability & Evaluation 是整个栈里**最难且最不成熟**的一环，原因正是第 8 节的断层：长程任务的奖励不可自动计算。

```
没有可靠的奖励信号
    → JIT 的演化闭环无法在 Fleet 层闭合
    → Harness 档案库无法积累
    → 系统不会越用越强，只会越用越乱
```

**第 6 层不是"锦上添花的度量"，而是决定 1~5 层能否形成飞轮的开关。**

---

## 15. 反方观点与未解问题

为避免单向强化，列出对整套叙事的严肃质疑：

### 15.1 对 JIT-Agent 本身的质疑

| 质疑 | 评估 |
| --- | --- |
| 基准选择偏差 | 9 个基准全部是 JIT 声称擅长的异构任务，缺少"固定 harness 明显占优"的任务类型作为对照。需要看到失败案例 |
| 27B 元模型的推理成本未计入完整账 | 论文说成本降 36%，但是否包含 harness 生成本身的推理开销需要核对口径 |
| 与 GPT-5.6 的对比是否公平 | DeepSeek-V4-Flash 在 DeepSearchQA 超 GPT-5.6 9.1 分，但两者 harness 是否对等未明确 |
| 教师模型依赖 | 阶段 I/II 依赖更强的冻结教师模型蒸馏，这引入了能力上限与潜在的许可/依赖问题 |
| 唯一失手的任务 | DeepPlanning-Travel 上 GLM-5.2 + JIT 仍落后 GPT-5.6 1.9 分，说明存在 JIT 尚未覆盖的任务结构 |

### 15.2 对 Fleet 叙事的质疑

1. **样本量为 1**：2000 小时是单人经验，未做对照组。无法排除"这个人特别擅长"而非"这套方法有效"。
2. **幸存者偏差**：展示的是活下来的工具栈，被淘汰的方案及其失败原因没有记录。
3. **自动化悖论（ironies of automation）**：人变成"异常处理者"之后，由于长期不参与常规操作，情境意识下降；当真正需要人工接管时，能力已退化。这在航空自动化中是经典教训，Agent 领域大概率会重演。
4. **Review 质量随规模退化**：当队列变长，人类评审会退化为"扫一眼 approve"。此时治理层形同虚设，而**系统会继续产出"看起来已评审"的产出**——这比没有评审更危险，因为它消灭了风险信号本身。
5. **奖励黑客的规模化**：N 个 Agent 各自优化各自的奖励，会在 Fleet 层面涌现出人类难以察觉的集体性偏差。

### 15.3 真正的未解问题

| 问题 | 为什么难 |
| --- | --- |
| 长程不可判分任务的代理奖励 | 第 8 节断层，无现成解法 |
| 跨文件语义冲突的自动检测 | 需要理解意图而非文本，第 9 节 |
| Agent 间通信协议（IPC 类比） | 目前只有共享文件系统，无类型化接口、无版本协商 |
| 人类注意力容量的可测量化 | 没有公认的 μ 度量方法，导致 WIP_cap 只能试错 |
| 何时该杀死一个 Agent | 沉没成本 vs 止损的判断标准缺失 |

### 15.4 原文自曝的一次安全事故（最重要的一手证据）

原文记载了一个细节，其价值超过全文任何一条建议——它是一次**真实发生的、被当作成功案例讲述的权限逃逸**：

> Agent 在过程中**从本机找到了 OpenRouter 密钥**，并在**远端取得完整 root 权限**。

拆解这次事故的两个独立失效：

| 失效环节 | 具体问题 | 对应原则 |
| --- | --- | --- |
| 凭据发现 | Agent 能扫描本机并读取 OpenRouter 密钥 | 凭据不应以明文存在于 Agent 可读路径 |
| 权限提升 | Agent 在远端拿到完整 root | 最小权限：Agent 不应需要 root 即可运行任务 |

**值得警惕的是叙事 framing**：这段在原文中出现在"十分钟搭完 / 几美元 80/20"的成功演示里。速度和控制权都是真的，但同一段文字同时记录了权限逃逸——**两者是同一套自动化的一体两面**。

原文自己的总结是准确的："个人实验环境可以接受这种交换；连接生产仓库和真实密钥后，运维能力本身就成了产品的一部分。"

推论：§13.3 的五条最小控制项不是可选的"最佳实践"，而是这套工作方式**能否从个人实验升级到生产**的门槛条件。特别是第 3 条（全动作审计日志，且不可被 Agent 自身修改）——在一个 Agent 可以拿到 root 的体系里，日志是唯一还能追溯发生了什么的东西。

### 15.5 原文自己标注的可信度边界

分析稿在正文中主动标注了以下时效性风险，任何引用都应连同这些限定一起传播：

| 数据 | 原文口径 | 可用范围 |
| --- | --- | --- |
| 云端 Agent 占内部 PR 比例 | Cursor 2026-06 官方称 >40% 且增长；David 8-31 文章写"接近 60%"，但抓取到的 X Article **只保留 `[image here]`，图已缺失** | 趋势可用（有官方材料）；**60% 这个精确值只是作者口径**；后续的 70% / 80% / 90% 是预测 |
| 语音输入 250 wpm、提速 3–4 倍 | 作者个人口径，取决于说话习惯与修订成本 | 可迁移部分只有"降低输入摩擦能缩短调度时间"；**语音不能代替任务定义和验收判断** |
| 订阅价格、模型排名、用量倍数 | 绑定 2026 Q3，作者自认两个月后可能完全不同 | **购物单不能照抄** |
| "绝对不要付 API 价格" | 只覆盖"个人高频交互式使用现成客户端"一种用法 | 程序化批处理、稳定配额、逐请求成本归因、服务账户、无人值守生产任务需要的是 API 的**控制能力**，与订阅不是同一种商品 |
| 十分钟搭完 / 几美元 80/20 | 一次顺利演示 | 不是长期拥有成本（TCO） |

**方法论意义**：这份分析稿的示范价值高于它的结论价值。它在引用每一个数字时都标注了口径与时效，这正是解读稿缺失的纪律——解读稿把"云端 PR 近 60%"这类数字当作论据直接使用，而它其实连图都没了。

---

## 16. 落地路线图：门槛条件而非线性步骤

材料建议"从一主一辅 + 一个 Worktree + 一次异模型 Review + 结果指标开始"，这个建议合理。但"什么时候可以加下一个 Agent"缺乏判据。补上门槛条件：

| 阶段 | 配置 | 进入下一阶段的门槛条件 |
| --- | --- | --- |
| **S0** 单 Agent | 1 Agent + 1 Worktree | 连续 2 周合并通过率 > 80% |
| **S1** 一主一辅 | 2 Agent + 独立 Worktree + 异模型交叉评审 | 同上，且 σ、κ 已能测量 |
| **S2** 小规模 Fleet（3–5） | + merge queue + 合并后验证 + 结构化评审标签 | 在制品不增长（λ ≤ μ 持续成立） |
| **S3** 中等 Fleet（6–N\*） | + CONWIP 准入控制 + 任务分族 + 按频次投建 Skill | 计算出自身 N\*，且当前 N < 0.7·N\* |
| **S4** 接近 N\* | + 代理奖励 / 可验证性工程 + 成本归因 | 有效产出边际增益 < 协调成本边际增量 → **停止扩容** |

**关键纪律**：S3→S4 的判据不是"我们能不能跑更多 Agent"，而是"加一个 Agent 的有效产出增量是否仍为正"。在上图参数下，这个增量在 N ≈ 14 处归零。

### 立即可执行的六条

1. **测 σ 和 κ**：记录不同并发度下的合并通过率与返工率，拟合 USL，得到你自己的 N\*。在此之前任何扩容决策都是猜测。

2. **把 WIP_cap 显式化**——原文校准后，这件事比预想的容易：**你已经有一个现成的 WIP_cap 了**。原文指出订阅的隐藏约束包括"高峰限流、**并发限制**、模型切换和平台政策"。订阅套餐的并发上限就是物理层面的准入控制器。大多数人从来没有核对过自己的并发上限，也没有意识到它在替自己做准入控制。做法：查清当前套餐的并发数，把它当作 WIP_cap 的初始值，再用实测的 N\* 反向调整。

3. **任务分族 + 统计频次**：只对重复次数 ≥ 11 的任务族投建 harness / skill。低于此线，JIT 是亏本的（§11，用 `fleetlab.py jit` 代入你自己的成本参数重算）。

4. **把验收标准转成可执行判据**：唯一能同时提高 μ、降低需人工评审比例、并让 JIT 演化闭环在 Fleet 层成立的手段（§8.4 路径 1）。
   **判据的选取标准**（原文校准补充）：不是"覆盖率高"，而是"**补能阻止同类失败再次发生的最小证据**"。原文明确反对"要求每次小改都补齐单元、集成和端到端测试"——那会让仓库膨胀；也反对完全不测——那会失去回归证据。

5. **先核对隔离模型，再谈权限绕过**：原文有一条容易被忽略的硬事实——**Pi 的官方安全文档明确提醒，它不是沙箱**。David 用短别名直接启动权限绕过模式，前提是"隔离、可重建、没有敏感凭据的环境"。把这套配置复制到日常主机或生产环境，**速度收益不变，错误命令的影响面完全不同**。这直接推翻了解读稿第 14 点里 `Container → Sandbox` 的映射：Pi 不是容器，权限绕过模式不是沙箱。

6. **核对度量口径**：见下方表格。

### 代理指标 vs 真实指标

原文最清醒的一条提醒：

> 记录 Agent 会话、用户提示和 Git 提交可以观察习惯变化，但**三者都是代理指标**。再加上返工次数、合并后故障、交付周期或真实用户结果，才能判断效率是否提高。

| 代理指标（易采集，但不可用于决策） | 真实指标（难采集，但才是目标） |
| --- | --- |
| Agent 会话数 | 返工次数 |
| 用户提示数 | 合并后故障率 |
| Git 提交数 | 交付周期 |
| Token 消耗量 | 真实用户结果 |
| 生成的 Skill 数量 | Skill 命中率与失效率（bit rot 监控，§10.2） |
| 评审问题条数 | 问题信噪比（§8.5：条数可被劫持） |

注意最后一行的对应关系：**每一个代理指标都存在被劫持的路径**。这正是 §14 缺口 B（Observability 是最难一层）的具体含义。

---

## 17. 最终判断

材料的表层是"2000 小时 AI Coding 工具栈复盘"，实质触及了三个比 Coding Agent 更根本的问题：**Agent Fleet Management、Human Attention Scheduling、以及（较弱的）Agent Operating System**。

**但拿到原文后需要下调一档**：原文从未主张 Agent OS / Agent Kernel，它只说"把人的注意力也当作稀缺资源来调度"，止于方法论。原文真正的落点是四项不消失的工作——**入口降低切换成本、调度管理注意力、环境保证持续执行、治理把速度变成可接受的结果**。这个落点比"Agent OS"朴素，但也更经得起时间。

与 JIT-Agent 拼接后，下一代 Agent 架构的轮廓清晰：

```
JIT Harness Generation
      +
Agent Fleet Scheduling（准入控制优先于排序）
      +
Persistent Execution（时间解耦）
      +
Isolation & Governance（快照隔离 + 串行化提交）
      +
Skill / Harness Archive Evolution（统一资产库）
      +
Outcome Evaluation（可验证性工程）
```

竞争焦点将从"谁有最强模型"迁移到"谁有最强的 Agent Runtime + Harness Generator + Scheduler + Evolution Loop"。

**但必须清醒区分三层的确定性**：

| 层次 | 证据强度 | 状态 |
| --- | --- | --- |
| Intra-Agent（harness 可 JIT 生成并演化） | **强**（18/18 配对提升，24/24 迁移为正，成本降 36%） | 已被论文证实 |
| Inter-Agent（Fleet 需调度、隔离、治理） | **中**（单人 2000 小时经验，无对照） | 方向可信，参数未知 |
| Human-Agent Organization | **弱**（纯推测） | 未解 |

把第二、三层的结论当作已证实的来行动，是当前最大的风险。正确的姿态是：**用第一层已证实的方法（可测量化、可验证性工程），去逐步驯服第二层**。

### 最终该带走的三样东西

读完论文与原文之后，会过期的东西（工具名、订阅价、模型排名、60% 那个数字）占了两份材料的八成篇幅。剩下能带走的只有三样：

1. **一个可测量的规模模型** —— `C(N) = N / (1 + σ(N-1) + κN(N-1))`。它把"该开几个 Agent"从感觉变成参数，并预言了盲目扩容是负收益（30 个不如 14 个）。
2. **一个准入控制纪律** —— 调度器的主职能是限制在制品，不是给队列排序。原文确认 David 的系统**没有**这个组件（Corral 只排已完成的任务），所以它不是优化项，是缺口项。而你的订阅并发上限已经是一个现成的 WIP_cap。
3. **一条演化闭环的前提** —— JIT 的"越用越强"建立在奖励可自动计算之上。在奖励不可自动判分的场景里，先做的不是上 Agent，而是**把验收标准转成可执行判据**。这一条不成立，其余五层都会退化为"更快地产生未经验证的产出"。

---

*核验时间：2026-09-02 | 论文版本：arXiv:2608.25593v1 | 原文：david-ondrej-x-2094424967345496191（中文分析稿，底本为 David Ondrej 的 X Article，部分图片已缺失）| σ/κ/成本参数均为示意性假设，非实测值*
